# CSC3060 Calling Convention 调用约定 完整笔记

> 用大白话讲清楚，考试能默写就行。

---

## 一、函数调用的完整步骤（Procedure Call Steps）

一个函数 A（Caller，调用者）调用另一个函数 B（Callee，被调用者），总共要干这些事：

1. **把参数放到栈/寄存器上**（Put parameters）
2. **保存返回地址**（Save return address）—— 记住"调用完了回哪"
3. **把控制权交给 B**（Transfer control to callee）—— 跳转过去
4. **在栈上推一个新帧**（Push activation record / stack frame）
5. **保存需要保存的寄存器**（Save registers）
6. **执行 B 的具体工作**（Do the work）
7. **把返回值放好**（Put return values）
8. **恢复之前保存的寄存器**（Restore saved registers）
9. **返回控制权给 A**（Return control to caller）

> **考试重点**：这 9 步的顺序和每一步在干什么，可能让你排序或填空。

---

## 二、栈帧结构（Stack Frame / Activation Record）

函数被调用时，会在栈上分配一块空间，叫做**栈帧（Stack Frame）**，也叫**激活记录（Activation Record）**。

### 栈帧的内存布局（从高地址到低地址）

```
高地址
┌──────────────┐
│  param 2     │  ← fp + 12
├──────────────┤
│  param 1     │  ← fp + 8
├──────────────┤
│ return addr  │  ← fp + 4   （返回地址）
├──────────────┤
│  old fp      │  ← fp       （上一个帧指针）
├──────────────┤
│  local 1     │  ← fp - 4   （局部变量）
├──────────────┤
│  local 2     │  ← fp - 8
├──────────────┤
│ saved regs   │             （保存的寄存器）
│  / temps     │
├──────────────┤
│              │  ← sp       （栈顶）
└──────────────┘
低地址
```

**核心要点**：

- **fp（Frame Pointer，帧指针）**：指向当前帧的固定位置，用来找参数和局部变量
- **sp（Stack Pointer，栈指针）**：指向栈顶，会随着 push/pop 变化
- 参数在 fp **上方**（正偏移），局部变量在 fp **下方**（负偏移）
- 栈是**向下增长**的（地址越来越小）

> **考试重点**：画出栈帧布局，标注 fp、sp、参数、返回地址、old fp、局部变量的位置和偏移量。

---

## 三、调用约定（Calling Convention）

### 什么是调用约定？

调用约定是一种实现方案，用于规定子程序如何接收参数、如何返回结果、如何设置函数调用以及调用后如何清理环境。

**调用约定（Calling Convention）** 是一套**软件层面的规则**，规定了：

- 参数怎么传（Parameters passing）
- 返回值怎么拿（Return values）
- 返回地址存哪（Return address）
- 栈怎么建、怎么拆（Stack setup & cleanup）
- 哪些寄存器谁来保存（Caller/Callee save registers）

> 关键理解：这是**软件约定**（Software Convention），不是硬件强制的。大家都遵守这个约定，代码才能互相调用（比如你写的代码调用别人的库）。如果你的代码是完全独立的、不调用任何外部代码，理论上可以不遵守。

---

## 四、RISC-V 寄存器约定（⭐考试核心⭐）

### 完整寄存器表

| 寄存器编号          | ABI 名字      | 用途                                                                         | 保存责任            |
| -------------- | ----------- | -------------------------------------------------------------------------- | --------------- |
| x1             | **ra**      | 返回地址（Return Address）(比如保留了函数返回的地址)                                         | Caller save     |
| x2             | **sp**      | 栈指针（Stack Pointer），永远指向栈顶，常使用 (sub sp,sp,C) 来表示从内存(栈)中分配C个字节的空间            | Callee save     |
| x3             | **gp**      | 全局指针（Global Pointer），可以理解为指向全局变量和静态变量的区域，方便快速查找                            | —               |
| x8             | **fp (s0)** | 帧指针（Frame Pointer）, 用处前面讲了                                                 | Callee save     |
| x10–x17        | **a0–a7**   | 函数参数 / 返回值（Arguments）(给函数传递8个及以内的参数,就会存在在这些寄存器里面,函数的返回值也可能用这些返回)           | Caller save     |
| x5–x7, x28–x31 | **t0–t6**   | 临时寄存器，谁都能用，谁都能改。如果A在里面存了东西，然后调用B，B可以直接改了里面的数据而不负责，对于A来说，调用B之前要先把这里面的东西存到内存 | **Caller save** |
| x8–x9, x18–x27 | **s0–s11**  | 保存寄存器（Saved），根据上面的例子，B是不能动 这几个寄存器的，所以A不用将里面的东西存到内存                         | **Callee save** |

> **a0 和 a1** 同时用来存放函数的**返回值**。

> **考试重点**：这张表必须背下来！特别是哪些是 caller save、哪些是 callee save。

---

## 五、Caller Save vs Callee Save 寄存器（⭐⭐考试最爱考⭐⭐）

### Caller Save（调用者保存）

**一句话**：这些寄存器的值，**调用者**如果还想用，就必须**自己在调用前保存、调用后恢复**。被调用的函数可以随便改，不用管。

- 包括：**t0–t6**（x5–x7, x28–x31）、**a0–a7**（x10–x17）、**ra**（x1）
- 用大白话说：公共座椅。你（Caller）手里拿着一个东西，你要去叫别人帮忙（调用函数），别人可能会把你手里的东西搞丢。所以**你自己先放好**，别人干完活你再拿回来。

### Callee Save（被调用者保存）

**一句话**：这些寄存器的值，**被调用的函数**如果要用，就必须**在函数开头保存、在返回前恢复**。保证调用者看到的值不变。

- 包括：**s0–s11**（x8–x9, x18–x27）、**sp**（x2）
- 用大白话说：保险柜。你（Callee）借了别人的东西用，**用完必须还回去**，不能弄丢。

### 图示理解

```
Caller Save (t0-t6):
  调用前：Caller 保存 → 调用函数 → 调用后：Caller 恢复
  
Callee Save (s0-s11):
  函数入口：Callee 保存 → 函数执行 → 函数返回前：Callee 恢复
```

---

## 六、经典例子分析（考试必考类型）

### 例1：Caller Save 的场景

```c
// 函数 A
int A() {
    t1 = a * b;         // 结果存在 $T1（caller save 寄存器）
    s2 = B(1, 2, 3);    // 调用 B
    c = t1 + s2;        // 还要用 t1！
}
```

问题：**$T1 是 caller save 寄存器**，调用 B() 之后 B 可能把 $T1 改了。 解决：**A 必须在调用 B 之前保存 $T1，调用 B 之后恢复 $T1**。

```
A() {
    t1 = a * b;
    SAVE t1          ← 调用前保存
    s2 = B(1,2,3);
    RESTORE t1       ← 调用后恢复
    c = t1 + s2;
}
```

### 例2：Callee Save 的场景

```c
// 函数 A
int A() {
    s1 = a * b;         // 用了 $S1（callee save 寄存器）
    s2 = B(1, 2, 3);    // 用了 $S2
    c = s1 + s2;
}
```

问题：A 修改了 $S1 和 $S2，这是 callee save 寄存器。谁调用 A 的时候，期望 $S1、$S2 不变。 解决：**A（作为 callee）在函数入口保存 $S1/$S2，在返回前恢复**。

```
A() {
    SAVE s1, s2       ← 函数入口保存
    s1 = a * b;
    s2 = B(1,2,3);
    c = s1 + s2;
    RESTORE s1, s2    ← 返回前恢复
}
```

**对于 callee-saved:**
**我们必须在调用之前先保存一次(save)，然后在用完之后恢复(restore)**

---

## 七、Caller Save vs Callee Save 的代价对比（⭐考试爱考⭐）

### 关键例子：一个变量跨越多次函数调用

```c
A() {
    x1 = a * b;
    B(1,2,3);    // 调用1
    C(4,a);      // 调用2
    D(x,y);      // 调用3
    ... = x1;    // 最后还要用 x1
}
```

**如果 x1 放在 Caller Save 寄存器（如 $T1）**：

- 每次调用前都要 save，调用后都要 restore
- 3 次调用 × 2 次操作 = **总代价 6 次**

**如果 x1 放在 Callee Save 寄存器（如 $S1）**：

- 只在函数入口 save 一次，返回前 restore 一次
- **总代价 2 次**

> **结论**：当一个值需要**跨越多次函数调用**存活（live across calls），用 **Callee Save 寄存器更省**。

---

## 八、寄存器分配的一般规则（General Rules）

| 变量类型                                    | 应分配到                 | 原因                            |
| --------------------------------------- | -------------------- | ----------------------------- |
| **临时变量**（Temporaries）                   | Caller Save (t0–t6)  | 临时变量**生命期短**，很快就用完了，不需要跨调用保存  |
| **局部变量**（Local Variables）               | Callee Save (s0–s11) | 局部变量**生命期长**，可能跨越整个函数，需要跨调用保存 |
| **公共子表达式**（CSE, Common Sub-Expressions） | Callee Save (s0–s11) | CSE 也是长寿命的，需要跨调用保存            |

> **考试重点**：这三条规则和**为什么**（Why），必须能说清楚。

---

## 九、特殊情况分析（考试常见题型）

### 情况1：没有值跨越函数调用

```assembly
lw  $8,  -4($fp)
lw  $9,  -8($fp)
add $10, $8, $9
sw  $10, -12($fp)
B(1,2,3)          ; 函数调用
add $v0, $v0, $v0
return
```

**问**：调用 B 需要保存哪些寄存器？ **答**：**不需要保存任何寄存器！** 因为调用 B 之后没有任何之前计算的值还需要被使用（no values live across the call）。

### 情况2：叶子函数（Leaf Routine）

**叶子函数** = 不调用任何其他函数的函数。

**问**：叶子函数应该用什么寄存器？ **答**：**尽量用 Caller Save 寄存器（t0–t6）**。因为它不调用别人，所以这些寄存器不会被别的函数破坏，不需要保存/恢复任何寄存器，零开销。

### 情况3：非叶子函数，但调用很少发生

```c
A() {
    if (common_case) {
        // 大量计算，不调用函数
    } else {  // 很少走到这
        B(); C(); D();
    }
}
```

**问**：应该用什么寄存器？ **答**：**用 Caller Save 寄存器**。因为 save/restore 只发生在不常走的 else 分支，大部分时间零开销。

这个例子有点阴间啊

---

## 十、理想世界 vs 现实世界

### 理想世界

- **Caller** 只用 Callee Save 寄存器 → 调用前不需要保存任何东西
- **Callee** 只用 Caller Save 寄存器 → 进入函数不需要保存任何东西
- 完美，零 save/restore 开销！

### 现实世界

- 一个函数**既是 Caller 又是 Callee**！（A 调用 B，同时 A 也被别人调用）
- 所以不可能完美，只能根据具体情况选择最优策略

---

## 十一、总结速查表（考前快速背诵）

| 概念                             | 要点                                  |
| ------------------------------ | ----------------------------------- |
| Calling Convention（调用约定）       | 一套软件规则，保证函数之间能正确协作                  |
| Stack Frame（栈帧）                | 每个函数调用在栈上分配的空间，存参数/返回地址/局部变量/保存的寄存器 |
| fp（Frame Pointer）              | 指向当前栈帧的固定位置，参数在 fp 上方，局部变量在下方       |
| sp（Stack Pointer）              | 指向栈顶，动态变化                           |
| Caller Save (t0–t6, a0–a7, ra) | 调用者负责保存，被调用者可以随便改                   |
| Callee Save (s0–s11, sp)       | 被调用者负责保存，保证调用者看到的值不变                |
| Leaf Routine（叶子函数）             | 不调用其他函数，优先用 caller save 寄存器，零开销     |
| Temporaries → Caller Save      | 短命，不跨调用                             |
| Locals/CSE → Callee Save       | 长命，跨调用                              |
| 跨多次调用                          | Callee Save 更省（save/restore 只做一次）   |

---

## 补充：RISC-V 常见指令集扩展 (Extensions) 速查表

| 扩展简称         | 英文全称 (Full Name)                    | 核心作用 / 功能说明                                                   | 默认指令长度  |
| :----------- | :---------------------------------- | :------------------------------------------------------------ | :------ |
| **I**        | Base Integer                        | **基础整数指令集**（必须包含），提供最基本的算术、逻辑、访存和分支控制功能。                      | 32位     |
| **E**        | Embedded                            | **嵌入式基础指令集**，为极致节省面积，**将通用寄存器从 32 个砍到 16 个**。                 | 32位     |
| **M**        | Integer Multiplication and Division | **整数乘除法**，包含硬件级别的乘法和除法指令。                                     | 32位     |
| **A**        | Atomic                              | **原子操作**，提供 Load-Reserved/Store-Conditional 等指令，用于多核并发同步和锁机制。 | 32位     |
| **F**        | Single-Precision Floating-Point     | **单精度浮点**，增加 32 个独立浮点寄存器，支持 32 位浮点运算。                         | 32位     |
| **D**        | Double-Precision Floating-Point     | **双精度浮点**，扩展浮点寄存器以支持 64 位浮点运算（依赖 F 扩展）。                       | 32位     |
| **Q**        | Quad-Precision Floating-Point       | **四精度浮点**，支持 128 位浮点运算（依赖 D 扩展）。                              | 32位     |
| **C**        | Compressed                          | **压缩指令**，将最常用的指令编码为 16 位，大幅减少代码体积（代码密度）。                      | **16位** |
| **B**        | Bit Manipulation                    | **位操作增强**，提供前导零计数、位翻转、提取等，加速密码学和底层位运算。                        | 32位     |
| **V**        | Vector Operations                   | **向量扩展**，支持数据级并行（类似 SIMD 但更灵活），用于高性能计算和机器学习加速。                | 32位     |
| **G**        | General-purpose                     | **通用组合简称**，它不是单个扩展，而是 `IMAFD`（加上 `Zicsr` 和 `Zifencei`）的统称。    | 混合      |
| **Zicsr**    | Control and Status Register         | **CSR 操作**，专门用于读写控制与状态寄存器的指令（以前包含在 I 中，后被独立）。                 | 32位     |
| **Zifencei** | Instruction-Fetch Fence             | **指令取指屏障**，用于确保写入内存的指令能被正确读取（清空流水线/缓存同步）。                     | 32位     |

> 只要记住了这些字母，以后看到类似 `RV32IMAC` 这样的名字，就能秒懂这是一个“32位基础整数 + 乘除法 + 原子操作 + 压缩指令”的处理器内核了。如果是做体系结构流水线设计，像 `M`（多周期执行）、`V`（复杂向量调度）和 `C`（不对齐取指）都会给指令调度和冒险处理带来非常大的挑战。