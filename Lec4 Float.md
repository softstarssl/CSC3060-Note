# 浮点数（Floating-Point Numbers）总结

## 1. 为什么需要浮点数？
- **整数不够用**：整数（int32最大约40亿，int64最大约9×10^18）无法表示非常大或非常小的数，比如天体距离、原子数量等。
- **科学计数法**：浮点数就像二进制的**科学计数法**，可以表示极大和极小的实数。

## 2. 二进制科学计数法（Binary Scientific Notation）
- **规格化（Normalized）形式**：$\pm 1.xxxxxx_2 \times 2^{yyyy}$
    - 小数点前总是`1`（二进制），这样表示是**唯一**的，且能**最大化精度**。
    - 例子：$0.0001_2 \times 2^{10}$（未规格化） -> 左移小数点，变成 $1.0_2 \times 2^{6}$（规格化）。
- **规格化好处**：
    1. 表示唯一。
    2. 最大化精度（用满所有位数）。
    3. 便于制定统一标准（IEEE 754）。

## 3. 二进制小数转换
### 3.1 二进制转十进制
每位乘以对应的2的负幂次，然后求和。
- 例子：$1.1011_2 = 1\times2^0 + 1\times2^{-1} + 0\times2^{-2} + 1\times2^{-3} + 1\times2^{-4} = 1.6875_{10}$

### 3.2 十进制小数转二进制
不断乘以2，取整数部分。
- 例子：转 $0.7_{10}$ 为二进制：
    1. $0.7 \times 2 = 1.4$ -> 整数部分`1`，剩下$0.4$
    2. $0.4 \times 2 = 0.8$ -> `0`
    3. $0.8 \times 2 = 1.6$ -> `1`
    4. $0.6 \times 2 = 1.2$ -> `1`
    5. $0.2 \times 2 = 0.4$ -> `0`（开始循环）
    - 所以 $0.7_{10} \approx 0.10110\overline{0110}_2$

## 4. IEEE 754 标准
为了解决不同计算机浮点数不兼容的问题，制定了IEEE 754标准。
- **两种主要精度**：
    1. **单精度（Single Precision, float）**：32位。
    2. **双精度（Double Precision, double）**：64位。

### 4.1 单精度（32位）格式
- **1位**符号（Sign, `s`）
- **8位**指数（Exponent, `exp`）
- **23位**尾数/小数部分（Fraction/Mantissa, `frac`）
- **公式**：$x = (-1)^s \times (1 + \text{frac}) \times 2^{(\text{exp} - 127)}$
    - 这里 $(1 + \text{frac})$ 叫做**有效数字（Significand）**。注意隐含的`1`（规格化数）。
- **指数偏移（Bias）**：指数`exp`是用**偏移表示（biased representation）** 的，单精度偏移量是**127**。
    - 好处：可以把浮点数当**整数**来比较大小（因为指数大的浮点数，其表示成的整数也大）。

### 4.2 双精度（64位）格式
- **1位**符号
- **11位**指数（偏移量是1023）
- **52位**尾数
- **公式**：$x = (-1)^s \times (1 + \text{frac}) \times 2^{(\text{exp} - 1023)}$

## 5. 特殊值和范围
### 5.1 规格化数（Normalized Numbers）
- **最大单精度数**：指数`exp`最大为254（因为255和254有特殊用途），尾数全1。
    - 值：约 $3.4028235 \times 10^{38}$
    - 位模式：`0 11111110 11111111111111111111111`
- **最小单精度数（规格化）**：指数`exp`最小为1，尾数全0。
    - 值：$2^{-126} \approx 1.175 \times 10^{-38}$
    - 位模式：`0 00000001 00000000000000000000000`

### 5.2 非规格化数（Denormalized/Subnormal Numbers）
- **是什么**：当指数`exp`全为0时，有效数字不再隐含`1`，而是`0`。用来表示**非常接近0**的数。
- **公式**：$x = (-1)^s \times (0 + \text{frac}) \times 2^{-126}$（单精度）
- （注意这里相当于是 $0.frac$ 而前面规格化数是 $1.frac$ ，并且其指数被固定成 $-126$ ！！ 
- **最小正非规格化数**：尾数最低位为1，其余为0。
    - 值：$2^{-23} \times 2^{-126} = 2^{-149} \approx 1.4 \times 10^{-45}$
    - 位模式：`0 00000000 00000000000000000000001`
- **作用**：提供**渐进下溢（gradual underflow）**，避免突然归零。

### 5.3 特殊位模式
- **指数全0，尾数全0**：表示**0**。有+0和-0。
- **指数全1，尾数全0**：表示**无穷大（Infinity）**。有+∞和-∞。
    - 例如：`1/0` 得到 ∞，`1/(-0)` 得到 -∞。
- **指数全1，尾数非0**：表示**非数（NaN, Not a Number）**。
    - 例如：$\sqrt{-1}$，`0/0`，∞ - ∞ 等。

## 6. 为什么浮点数范围大但精度不固定？
- **范围大**：因为指数部分可以变得很大或很小。
- **精度不固定**：尾数位数固定（单精度23位）。数值越大，两个相邻可表示数之间的**间隔（gap）** 也越大。
    - 例子：单精度只能精确表示整数到 $2^{24} = 16,777,216$。超过这个数，有些整数就无法精确表示，必须**舍入（rounding）**。
    - 就像望远镜：看远处（大数）范围大，但看不清细节（精度低）；看近处（小数）范围小，但看得清（精度高）。

## 7. 浮点数舍入（Rounding）
浮点数运算结果必须舍入到最接近的可表示值。
- **IEEE 754 四种舍入模式**：
    1. **向偶数舍入（Round to nearest, ties to even）**：默认模式，最常用的那个。如果刚好在两个可表示值中间，就选尾数为偶数（最低位为0）的那个。
        - 例子：12.5（二进制1100.1）介于12(1100)和13(1101)之间，选12（偶数）。
        - 13.5 则会舍入到 14
        - 人话说就是四舍五入，但是.5会向偶数方向舍入，所以可能比原数小，也可能比原数大
    2. **向零舍入（Round toward zero）**：直接截断。
    3. **向下舍入（Round down）**：向负无穷方向舍入。
    4. **向上舍入（Round up）**：向正无穷方向舍入。

## 8. 浮点数运算（Floating-Point Operations）
### 8.1 加法（以十进制4位数为例：$9.999\times10^1 + 1.610\times10^{-1}$）
1. **对齐小数点（Align exponents）**：将小指数调整到大指数。
    - $1.610\times10^{-1} = 0.016\times10^1$
2. **尾数相加（Add significands）**：
    - $9.999 + 0.016 = 10.015$ -> $10.015\times10^1$
3. **规格化结果（Normalize）**：
    - $10.015\times10^1 = 1.0015\times10^2$
4. **舍入（Round）**：
    - 四舍五入到4位数：$1.002\times10^2$

### 8.2 乘法
步骤更简单：
1. **指数相加**。
2. **尾数相乘**。
3. **规格化和舍入**。

### 8.3 重要性质
- **可交换（Commutative）**：$a + b = b + a$，$a \times b = b \times a$。
- **不可结合（Not Associative）**：$(a + b) + c \neq a + (b + c)$。
    - 例子（单精度）：$(3.14 + 1e10) - 1e10 = 0.0$，但 $3.14 + (1e10 - 1e10) = 3.14$。
    - 因为大数吃掉小数。
- **不可分配（Not Distributive）**：$a \times (b + c) \neq a \times b + a \times c$。

## 9. 类型转换（Type Casting）
- **int -> float**：可能被舍入（因为float尾数只有23位），但不会溢出（float范围远大于int）。
- **int/float -> double**：可以精确表示（double精度更高）。
- **double -> float**：可能溢出（得到±∞）或舍入（精度损失）。
- **float/double -> int**：向零舍入（直接去掉小数）。可能溢出，C语言未定义结果，但x86会生成一个特殊的“不确定整数”值（bit pattern: 10...00）。

## 10. 浮点数硬件（Floating-Point Hardware）
- **浮点运算器（FPU）** 比整数运算器复杂得多，通常需要多个时钟周期。
- **可以流水化（pipelined）**：虽然单个操作慢，但可以同时处理多个操作，提高吞吐量。
- **支持的操作**：加、减、乘、除、开方、倒数、类型转换等。
### 浮点乘法硬件
- **浮点乘法不需要“对齐指数”这一步**  
    做浮点加法时，要先把两个数的小数点对齐（指数对齐），再相加。  
    但做浮点乘法时不用这一步，直接上来就算。
    
- **同时干两件事：部分积 + 指数相加**
    
    - 尾数部分要做乘法，会产生很多“部分积”（就是一堆中间乘法结果）。
    - 指数部分直接把两个指数加起来。  
        这两件事可以**并行**做，一边算乘法，一边把指数相加，提高速度。
- **后面还有几步收尾：**
    
    - 把很多部分积“压缩”成一个结果（reduction）。
    - 把结果规格化（normalization），比如变成 1.xxx×2e1.xxx \times 2^e1.xxx×2e 的标准形式。
    - 最后做一次舍入（rounding），保证符合 IEEE 754 的精度要求。
- **耗时：一般要 4–5 个时钟周期**  
    浮点乘法比浮点加法多做一点事，所以**比浮点加法多用大概 1 个周期**。

### 10.1 融合乘加（FMA / FMAC）
- **是什么**：把乘法和加法合并成一条指令：$a \times b + c$。
- **好处**：
    1. **更快**：原本需要两次运算（乘、加），每次计算都会舍入一次，共两次，现在只需要一次。
    2. **更精确**：中间结果不单独舍入，由硬件直接算出乘法的高精度结果，用这个结果和 $c$ 执行硬件加法，只在最后舍入一次，减少误差。
    3. **更省电**：指令数减少。
- **编译注意**：默认不开启，需要加编译选项（如gcc的 `-mfma -ffp-contract=fast`）。

## 11. 其他浮点格式
- **半精度（Half precision, fp16）**：1符号位，5指数位，10尾数位。用于AI，节省内存和带宽。1偏
- **bfloat16**：1符号位，8指数位（和float一样），7尾数位。范围同float，精度低，也用于AI。
- **四精度（Quad precision）**：1符号位，15指数位，112尾数位。用于需要超高精度的科学计算。
- **迷你浮点（Mini-float, fp8）**：1符号位，4指数位，3尾数位。极端压缩，用于特定AI推理。

## 12. 考试最爱考！
1. **给一个浮点数的二进制/十六进制表示，求十进制值**（套公式：$x = (-1)^s \times (1 + \text{frac}) \times 2^{(\text{exp} - \text{bias})}$）。
2. **给一个十进制数，求IEEE 754单/双精度表示**（先转二进制科学计数法，再确定s、exp、frac）。
3. **识别特殊值**（全0、全1的指数和尾数表示什么？）。
4. **计算浮点数加/乘法步骤**（对齐、计算、规格化、舍入）。
5. **解释为什么 $(a+b)+c \neq a+(b+c)$**（结合律不成立）。
6. **舍入模式**，特别是“向偶数舍入”的具体例子。
7. **类型转换的结果和可能问题**（溢出、舍入）。
8. **规格化和非规格化数的区别和表示范围**。

记住：浮点数是**近似**表示，有**范围**和**精度**限制。运算有误差，不满足结合律和分配律。硬件实现复杂但可以通过流水线加速。