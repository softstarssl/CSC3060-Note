
首先是装个 docker 花了半个下午，唐完了

## Practice 的答案

**Phase_1**：Genshin Impact makes me happy and Mini World makes my day.
神他妈原神和迷你世界

**Phase_2** 10 12 0 22

这是一个 switch 语句的操作，具体懒得写了
## 有关寄存器的记录

**ra**: 对应编号 x1,存放返回地址，比如函数运行结束后返回的地址
**sp**: 栈指针，指向当前栈顶， `sub sp sp x` 就是从栈上面分配 x 字节的地址
**a0,a1...a7** 可能是函数的第1,2...8个参数/返回值，要处理更多的参数和返回值的话就不用寄存器而是用栈了
**s0,s1...s11**: 保存寄存器，用来存数字的。特别的,s0 也可以用来作为帧指针(这是软件 ABI 的通俗约定)，比如可以用来标记某个函数的起始位置，然后丢到内存里面。由于栈指针 sp 经常移动，所以 s0 就承担了这样类似保存函数开始地址的任务.


## gdb 神秘小指令，AI 也干了

查看某个地址的东西： x/NFU Address

| 写法  | 含义             | 说明                                       |
|-------|------------------|--------------------------------------------|
| N     | repeat count     | 重复次数，不写默认为 1，如 `x/4wx` 看 4 项 |
| F     | format           | 显示格式，见下表                           |
| U     | unit size        | 每项字节数，见下表                         |
| ADDRESS | 起始地址       | 如 `$sp`, `$pc`, `0x1234`, `&var`          |

| F   | 含义           | 显示形式            | 常见用途          |
| --- | ------------ | --------------- | ------------- |
| x   | hex          | 十六进制            | 看指针、地址、原始内存   |
| d   | signed dec   | 有符号十进制          | 看带符号整数        |
| u   | unsigned dec | 无符号十进制          | 看无符号整数        |
| t   | binary       | 二进制             | 看位模式、bit 操作   |
| o   | octal        | 八进制             | 几乎不用          |
| a   | address      | 地址 + 符号名        | 看函数/变量的符号地址   |
| i   | instruction  | 反汇编一条指令         | 查看机器码对应的汇编    |
| c   | char         | 单个 ASCII 字符     | 看字符数组 / 字节流   |
| s   | string       | C 字符串（直到 `\\0`） | 看 `char*` 字符串 |
| f   | float        | float/double    | 看浮点数          |

| U   | 字节数 | 含义                   | 常见用途                 |
| --- | --- | -------------------- | -------------------- |
| b   | 1   | byte                 | 看原始字节、char、标志位       |
| h   | 2   | halfword (2 bytes)   | 看 short、半字           |
| w   | 4   | word (4 bytes)       | 看 32 位 int / 指针 / 指令 |
| g   | 8   | giant word (8 bytes) | 看 64 位值、双字           |


## Bomb15 的答案

### phase_1
 其实就是比较和 624 是否相等罢了
### phase_2
满足规律的六个数字
明早起来看
