# CSC3060 代码调度 (Code/Instruction Scheduling) 知识点总结

这个ppt已经被最新版本的 Hazards ppt 包含了，就很神秘

## 一、为什么要做代码调度？(Why Rearrange Code?)

流水线 (Pipeline) 执行指令时有 5 个阶段：**IF → ID → EX → Mem → WB**

|阶段|英文全称|干啥的|
|---|---|---|
|IF|Instruction Fetch|取指令|
|ID|Instruction Decode|解码 + 读寄存器|
|EX|Execute|执行运算|
|Mem|Memory Access|访问内存|
|WB|Write Back|写回寄存器|

**问题**：指令之间存在**数据冒险 (Data Hazard)** 和**控制冒险 (Control Hazard)**，会在流水线中产生**气泡 (Bubble)**——也就是空等的周期，白白浪费时间。

**解决思路**：不改变程序结果的前提下，**重新排列指令顺序**，把那些互不依赖的指令"插"到气泡的位置，减少空等。

> 💡 **一句话理解**：就像你在等外卖的时候先去洗个碗，而不是干坐着等——把空闲时间利用起来。

---

## 二、依赖关系 DAG (Dependence DAG)

### 2.1 什么是 DAG？

**DAG = Directed Acyclic Graph（有向无环图）**

把一个基本块 (Basic Block) 里的每条指令画成一个**节点 (Node)**，指令之间的依赖关系画成一条**有向边 (Arc/Edge)**。

- **节点** = 一条指令
- **边** = 一个依赖关系（"我必须等你做完才能开始"）

### 2.2 三种寄存器数据依赖 (Three Types of Register Data Dependence)

这是**考试重点中的重点**，必须能默写！

|类型|英文全称|缩写|含义|通俗解释|
|---|---|---|---|---|
|流依赖|Flow Dependence|**RAW** (Read After Write)|后面的指令要**读**前面指令**写**的值|"你先算出来，我才能用"|
|反依赖|Anti-Dependence|**WAR** (Write After Read)|后面的指令要**写**前面指令**读**的寄存器|"你先读完，我才能覆盖"|
|输出依赖|Output-Dependence|**WAW** (Write After Write)|两条指令**写同一个**寄存器|"我俩都要写，得排个先后"|

在 DAG 图中，不同类型用不同线型表示：

- **RAW（流依赖）**：实线箭头 →
- **WAR（反依赖）**：点划线箭头 -·-·→
- **WAW（输出依赖）**：点线箭头 ····→

> 💡 **记忆口诀**：RAW 是"真"依赖（数据真的要传递），WAR 和 WAW 是"假"依赖（可以通过寄存器重命名消除）。

### 2.3 内存依赖 (Memory Dependency)

除了寄存器依赖，还有**内存依赖**——当两条指令可能访问同一个内存地址时（由**指针别名 / Pointer Aliasing** 引起）。

内存指令调度的难点：

- 内存依赖关系可能**编译时未知**（指针指向哪里不确定）
- 内存访问延迟**不确定**（cache 命中 vs 不命中差别巨大）
- DRAM、总线等资源是**共享**的
- 编译器必须采取**保守策略** (conservative)——宁可不优化，也不能搞错

---

## 三、合法调度 (Legal Schedule)

### 3.1 拓扑排序 (Topological Sort)

**任何 DAG 的拓扑排序都是一个合法调度。**

拓扑排序的定义：对 DAG 中的节点进行排列，保证**每个节点都出现在它所有子节点之前**。

> 💡 **通俗理解**：排课表——高等数学排在数值分析前面，因为数值分析依赖高数。

同一个 DAG 可能有**多个**合法的拓扑排序。比如对 PPT 中的例子：

- `1,2,3,5,9,4,8,6,10,11,7` ✅ 合法
- `9,1,2,3,4,10,6,5,8,7,11` ✅ 也合法

### 3.2 调度爆炸问题

N 条指令的排列组合最多有 **N!** 种。

| 指令数 N | 可能的排列数 N! |
| ----- | --------- |
| 50    | 3 × 10⁶⁴  |
| 100   | 9 × 10¹⁵⁸ |

所以不可能暴力枚举所有调度——必须用**启发式算法 (Heuristic)**。

---

## 四、操作延迟与关键路径 (Operation Latency & Critical Path)

### 4.1 操作延迟 (Operation Latency) = 边的权重 (Weight of Arcs)

DAG 的每条边有一个**权重**，表示源指令执行完成需要多少个时钟周期，后续指令才能使用它的结果。

PPT 中的假设（**考试常用这组数据**）：

|指令类型|延迟(cycles)|
|---|---|
|Load（lw）|**2**|
|Multiply（mul）|**3**|
|Add / Store / Branch 等|**1**|

### 4.2 关键路径 (Critical Path)

**关键路径** = DAG 中从根节点到叶节点的**最长路径**（权重之和最大的路径）。

**原则：关键路径上的节点应该尽早被调度。**

### 4.3 ⭐ 最大延迟 MD 的计算公式（考试必考！）

从 DAG 的**叶节点**（没有子节点的节点）开始，**反向**（逆拓扑序 / Reverse Topological Sort）计算每个节点的最大延迟 (Maximum Delay, MD)：

$$ \boxed{MD(n) = \max_{\text{子节点 } c} \left( MD(c) + \text{arc\_weight}(n \to c) \right)} $$

- **叶节点**的 MD = 0（已经到终点了，没有后续延迟）
- **非叶节点**：看它所有子节点，挑"子节点的 MD + 到子节点的边权"中最大的那个

#### 完整计算示例（PPT 中的 11 条指令）

先确认叶节点（没有任何依赖于它的后续指令的节点）：

> 叶节点 = {6的子节点7, 10, 11, 7} → MD 都为 0

然后从叶往根反推：

|节点|指令|计算过程|MD|
|---|---|---|---|
|7|sw $pr6, 0($pr5)|叶节点|**0**|
|10|sw $pr7, 0($pr4)|叶节点|**0**|
|11|add $pr8,$pr7,$pr1|叶节点|**0**|
|6|add $pr6, $pr4, 2|max(MD(7)+1) = 0+1|**1**|
|8|lw $pr1, c|max(MD(11)+2) = 0+2|**2**|
|9|lw $pr7, d|max(MD(10)+2, MD(11)+2) = 2|**2**|
|5|mul $pr5, $pr1,$pr2|max(MD(7)+3, MD(8)+3) = max(3,5)|**3**¹|
|4|mul $pr4, $pr3, 3|max(MD(6)+3, MD(10)+3) = max(4,3)|**4**|
|3|add $pr3, $pr1,$pr2|max(MD(4)+1) = 4+1|**5**|
|1|lw $pr1, a|max(MD(3)+2, MD(5)+2) = max(7,5)|**7**|
|2|lw $pr2, b|max(MD(3)+2, MD(5)+2, MD(9)+2) = max(7,5,4)|**7**|

> ¹ 注：节点 5 的 MD 计算需要看具体边的权重，不同依赖类型可能权重不同。PPT 中标注 5 的 MD=3。

**关键路径**就是 MD 值最大的路径：1→3→4→6→7（或 2→3→4→6→7），总延迟 = **7**。

---

## 五、⭐ 列表调度算法 (List Scheduling Algorithm)

**这是本讲最核心的算法，考试大概率要手动模拟！**

### 5.1 算法步骤

```
1. 构建依赖 DAG (Build Dependence DAG)
2. 计算每个节点的 MD 值（从叶到根的反向拓扑排序）
3. 从 DAG 的根节点开始，维护一个"候选列表 (Candidate List)"
4. 每个周期，从候选列表中选一个节点调度，跟踪当前周期以减少 stall
```

### 5.2 ⭐ 选择启发式 (Heuristics)——按优先级从高到低

当候选列表中有多个节点可选时，按以下规则**依次打破平局**：

|优先级|规则|通俗解释|
|---|---|---|
|1|选**当前能立即执行不会 stall** 的节点|别选还要等的|
|2|平局→选 **MD 值最大**的节点|关键路径上的先做|
|3|平局→选**原始顺序靠前**的节点|保持原来的顺序|
|4|平局→选**子节点最多**的节点|做完它能解锁更多|
|5|平局→选**最后一次使用某寄存器**的节点|尽早释放寄存器|

### 5.3 完整调度过程模拟（PPT 中的例子）

**初始候选列表** = {1, 2, 9}（没有父节点/前驱的节点）

|步骤|候选列表|选谁？|理由|
|---|---|---|---|
|1|{1, 2, 9}|**1**|1和2的MD都是7，并列最高，选原始顺序靠前的1|
|2|{2, 9}|**2**|MD=7 > MD(9)=2|
|3|{3, 5, 9}|**9**|此时3和5还要等lw的结果(stall)，9可以立即执行，规则1优先|
|4|{3, 5}|**3**|MD(3)=5 > MD(5)=3|
|5|{4, 5}|**4**|MD(4)=4 > MD(5)=3|
|6|{6, 10, 5}|**5**|需看是否stall；如果都能执行，MD(5)=3 > MD(6)=1, MD(10)=0|
|7|{6, 10, 8}|**8**|lw指令，MD(8)=2 > MD(6)=1 > MD(10)=0|
|8|{6, 10, 11}|**6**|MD(6)=1 > MD(10)=0 = MD(11)=0|
|9|{10, 11, 7}|依次选 10, 7, 11|按规则依次选完|

### 5.4 调度前后效果对比

|对比|原始代码|调度后|
|---|---|---|
|完成周期|**13**|**10**|
|提升|—|少了 3 个周期！|

原始代码因为 lw 后紧接着用其结果，产生了多个气泡/stall；调度后把不相关的 lw 提前插入，填满了空闲周期。

---

## 六、核心概念速查表（考前默写用）

|概念|英文|一句话定义|
|---|---|---|
|代码调度|Code/Instruction Scheduling|在不改变程序语义的前提下重排指令顺序，减少流水线气泡|
|依赖DAG|Dependence DAG|用有向无环图表示指令间的依赖关系|
|流依赖|Flow Dependence (RAW)|先写后读，真依赖|
|反依赖|Anti-Dependence (WAR)|先读后写，假依赖|
|输出依赖|Output-Dependence (WAW)|都写同一个，假依赖|
|内存依赖|Memory Dependency|load/store之间因地址可能相同而产生的依赖|
|别名|Aliasing|两个指针可能指向同一个内存地址|
|拓扑排序|Topological Sort|DAG节点排列，父节点在子节点前|
|操作延迟|Operation Latency|一条指令完成所需的时钟周期数|
|关键路径|Critical Path|DAG中权重和最大的路径|
|最大延迟|Maximum Delay (MD)|某节点到DAG末端的最长加权距离|
|列表调度|List Scheduling|基于启发式的贪心调度算法|
|候选列表|Candidate List|所有前驱已调度完的、可以被调度的节点集合|
|气泡|Bubble / Stall|流水线中因冒险而插入的空周期|

---

## 七、考试必记公式 & 算法

### 公式 1：最大延迟 MD

$$ MD(n) = \begin{cases} 0 & \text{如果 } n \text{ 是叶节点} \ \max\limits_{\text{子节点 } c} \left( MD(c) + \text{weight}(n \to c) \right) & \text{否则} \end{cases} $$

### 算法：列表调度流程

```
输入：一个基本块的指令序列
输出：一个重排后的指令序列

1. 对所有指令建立依赖 DAG
2. 对每条边标注操作延迟作为权重
3. 用反向拓扑排序计算每个节点的 MD
4. 初始化候选列表 = 所有没有前驱的节点
5. WHILE 候选列表不为空:
     a. 从候选列表中按启发式规则选出一个节点 n
        (不stall > MD大 > 原始顺序 > 子节点多 > 释放寄存器)
     b. 调度 n，记录其发射周期 (issue cycle)
     c. 更新当前周期（考虑 n 的执行延迟）
     d. 将 n 的所有"前驱全部已调度"的子节点加入候选列表
6. 输出调度结果
```