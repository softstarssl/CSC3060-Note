# CSC3060 第四章：流水线实现（Pipelined Implementation）


## 1. 为什么要流水线？——三种实现方式对比

CPU 执行指令有三种方式，就像吃饭：

| 方式                    | 类比                 | 特点                          |
| --------------------- | ------------------ | --------------------------- |
| **单周期**（Single-cycle） | 一个人做完一整桌菜再开始下一桌    | 一条指令一个长周期，时钟周期 = 最慢指令的时间    |
| **多周期**（Multi-cycle）  | 一个人一步一步做菜，每步一个短周期  | 每条指令用不同数量的短周期，但同一时刻只有一条指令在跑 |
| **流水线**（Pipeline）     | 流水线饭堂，打饭、加菜、结账同时进行 | 多条指令重叠执行，**吞吐量大幅提升**        |

### ⭐ 考试核心数据（课件原例）

假设各阶段延迟：寄存器读/写 = 100ps，其他（MEM、ALU）= 200ps

|指令|IF(200)|ID(100)|ALU(200)|MEM(200)|WB(100)|总计|
|---|---|---|---|---|---|---|
|add|✓|✓|✓||✓|600ps|
|beq|✓|✓|✓|||500ps|
|jal|✓|✓|✓||✓|600ps|
|lw|✓|✓|✓|✓|✓|800ps|
|sw|✓|✓|✓|✓||700ps|

**关键结论：**

- **单周期时钟周期** = 最慢指令时间 = **800ps**（由 lw 决定），最大频率 = 1/800ps = **1.25 GHz**
- **多周期时钟周期** = 最慢阶段时间 = **200ps**，最大频率 = 1/200ps = **5 GHz**
- **流水线时钟周期** = 最慢阶段时间 = **200ps**，稳态下每 200ps 完成一条指令

---

## 2. 流水线的五个阶段（Five Stages）

这是**必须背的**，考试默写概率极高：

| 阶段  | 英文全称                               | 缩写      | 干什么（大白话）              |
| --- | ---------------------------------- | ------- | --------------------- |
| 取指  | Instruction Fetch                  | **IF**  | 从指令存储器里把指令取出来         |
| 译码  | Instruction Decode & Register Read | **ID**  | 翻译指令，同时读寄存器的值         |
| 执行  | Execute / Address Calculate        | **EX**  | ALU 做运算或算地址           |
| 访存  | Memory Access                      | **MEM** | 读/写数据存储器（只有 lw/sw 用到） |
| 写回  | Write Back                         | **WB**  | 把结果写回寄存器              |

### 流水线执行示意（必考图）

```
时钟周期:  1    2    3    4    5    6    7    8
指令1:    IF   ID   EX  MEM   WB
指令2:         IF   ID   EX  MEM   WB
指令3:              IF   ID   EX  MEM   WB
指令4:                   IF   ID   EX  MEM   WB
```

**核心理解**：每个周期都有一条新指令进入流水线，同时有一条指令完成。稳定状态下，**每个周期完成一条指令**。

---

## 3. 流水线加速比（Pipeline Speedup）⭐必考公式

### 理想情况（各阶段时间相等）

$$\text{加速比(Speedup)} = \text{流水线级数(Number of stages)}$$

也可以写成：

$$\text{流水线每条指令时间} = \frac{\text{非流水线每条指令时间}}{\text{流水线级数}}$$

### 实际情况

- 各阶段时间**不均衡** → 时钟周期由**最慢阶段**决定 → 加速比 < 级数
- **填充（fill）和排空（drain）** → 流水线刚启动和快结束时没有满载
- **数据/控制依赖** → 导致流水线气泡（bubble），降低吞吐量
- 多周期的时候，上面用的是每条指令的平均时间
- 还有这事byd理想状态，真实状态公式没给，看笔记和 ppt 的example 理解一下就好了

### 延迟 vs 吞吐量（Latency vs Throughput）⭐

| 类型                  | 单周期        | 流水线                           |
| ------------------- | ---------- | ----------------------------- |
| **延迟**（一条指令从头到尾的时间） | 800ps      | 200ps × 5 = **1000ps**（反而更慢！） |
| **吞吐量**（每隔多久完成一条指令） | 每 800ps 一条 | 每 **200ps** 一条                |

> **关键考点**：流水线**不减少**单条指令的延迟（甚至可能增加），但**大幅提高吞吐量**。

## 4. Exercise-1 类型题解法（考试计算题模板）⭐

课件给出一类经典题目，假设各阶段延迟不同（如 IF=250, ID=350, EX=150, MEM=300, WB=200），指令比例已知。

### Q1：求时钟周期

- **非流水线（单周期）**：时钟周期 = 所有阶段之和 = 250+350+150+300+200 = **1250ps**
- **流水线**：时钟周期 = 最慢阶段 = max(250,350,150,300,200) = **350ps**

### Q2：求 lw 指令延迟

- **非流水线**：1250ps（一个长周期就搞定）
- **流水线**：350 × 5 = **1750ps**（5个流水线周期，每个350ps）

### Q3：拆分哪个阶段？新的周期是多少？

- 拆**最慢的阶段**（瓶颈阶段），这里是 ID = 350ps
- 拆成两个子阶段后，新的最慢阶段变成 MEM = 300ps
- 新时钟周期 = **300ps**

### Q4：与多周期实现比较加速比

假设指令比例：lw 占 20%，ALU 占 45%，beq+sw 占 35%

多周期平均 CPI = 0.2×5 + 0.45×4 + 0.35×3 = 1.0 + 1.8 + 1.05 = **3.85**，这正是流水线与多周期的加速比

单周期 vs 流水线加速比 = 1250 / 350 = **3.57**

---

## 5. RISC-V 为什么适合流水线？（Pipeline-oriented ISA Design）

RISC-V 的设计天然适配流水线，考试可能出简答题：

1. **固定指令长度**（Fixed-length instructions）：全是 32 位，取指和译码简单。对比 x86 指令长 1~17 字节，取指很麻烦
2. **格式少且规则**（Few and regular instruction formats）：可以在一步内完成译码+读寄存器
3. **只有 load/store 访问内存**（Only load/store access memory）：第 3 阶段算地址，第 4 阶段访存，分工明确
4. **指令简单**（Simple instructions）：不需要太多执行步骤

---

## 6. 流水线寄存器（Pipeline Registers）⭐⭐必考

### 为什么需要流水线寄存器？

单周期数据通路上，同一时刻只有一条指令。但流水线上**同时有 5 条指令在跑**，它们共用同一个硬件。问题来了：

- 写回阶段的 Rd 到底是哪条指令的？
- 立即数从哪条指令取？
- rs1/rs2 该读哪条指令的？

**解决方案**：在每两个阶段之间插入**流水线寄存器**（Pipeline Register），用来保存该指令在当前阶段产生的所有信息，传递给下一阶段。

### 四组流水线寄存器

```
IF → [IF/ID] → ID → [ID/EX] → EX → [EX/MEM] → MEM → [MEM/WB] → WB
```

|流水线寄存器|存什么|
|---|---|
|**IF/ID**|取到的指令、PC+4|
|**ID/EX**|寄存器值、立即数、控制信号、PC、rd 编号|
|**EX/MEM**|ALU 结果、分支目标地址、控制信号、rd 编号|
|**MEM/WB**|从内存读出的数据或 ALU 结果、控制信号、rd 编号|

> **考点**：控制信号在 ID 阶段产生后，会随着流水线寄存器**一级一级往后传**，到了该用的阶段才拿出来用。

---

## 7. 控制信号的传递（Control Signals in Pipeline）⭐

在单周期中，控制信号在指令译码后立刻到达所有部件。但在流水线中，不同阶段执行的是不同指令，控制信号必须跟着**自己那条指令走**。

做法：控制信号在 ID 阶段生成后，存入 ID/EX 寄存器，然后每过一个阶段往后传一级：

```
ID产生 → 存入ID/EX → 传到EX/MEM → 传到MEM/WB
```

- EX 阶段用的信号（如 ALUsrc, ALUop）从 **ID/EX** 中取
- MEM 阶段用的信号（如 MemRead, MemWrite）从 **EX/MEM** 中取
- WB 阶段用的信号（如 RegWrite, WBsel）从 **MEM/WB** 中取

---

## 8. 长延迟操作的处理（Long Latency Operations）

比如除法（DIV）需要很多个周期，怎么办？两种方法：

1. **阻塞（Stall）**：后面的指令在 EX 阶段等着，整条流水线卡住
2. **独立的长延迟流水线**：让后续指令继续走，长操作在单独的功能单元里慢慢做。但要注意：
    - 指令通常按顺序在 WB 阶段退出（因为可能有异常/陷阱 trap）
    - RISC-V 避免算术异常，所以**乱序退出（out-of-order retirement）是可行的**

---

## 9. 流水线无处不在（Pipeline Everywhere）

流水线思想不只用在 CPU 指令执行，还用在很多地方（了解即可）：

- **DRAM 流水线**：行激活 → 列选择 → 数据传输
- **Cache 流水线**：地址译码 → Way 选择 → Tag 匹配 → 数据读取
- **浮点单元流水线**：指数对齐 → 运算 → 规格化 → 舍入
- **GPU 流水线**：顶点处理 → 光栅化 → 片段处理
- **网络流水线**：包头处理 → 路由表查找 → 包转发

> **一句话总结**：只要你需要更高的吞吐量，就考虑流水线。

---

## 10. 流水线核心要点总结（Pipeline Lessons）⭐考试简答

1. 流水线**提高吞吐量**（Throughput），**不降低**单条指令延迟（Latency）
2. 流水线速率受**最慢阶段**限制
3. 理想/潜在加速比 = 流水线级数
4. 填充和排空（fill & drain）会降低加速比（如 I-cache miss、分支预测错误）
5. 数据依赖和控制依赖会产生气泡（bubble），需要避免

---

## 必背清单 Checklist

- [ ] 五个阶段的名称、缩写、功能
- [ ] 流水线时序图画法
- [ ] 加速比公式 + "流水线不减少延迟只提高吞吐量"
- [ ] 四组流水线寄存器的名称和存储内容
- [ ] 控制信号如何随流水线寄存器传递
- [ ] RISC-V 适合流水线的四个设计特点
- [ ] 单周期/多周期/流水线的时钟周期计算方法
- [ ] Exercise-1 类型计算题的解题步骤