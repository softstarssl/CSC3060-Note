# CSC3060 - Lecture 1: Introduction (傻瓜复习版)

**课程:** CSC3060 Computer Systems | **主题:** 计算机系统导论
**核心宗旨:** 这节课是带你把计算机的“五脏六腑”看一遍，从硬件到软件，再到它们怎么配合。

---

## 1. 摩尔定律 (Moore’s Law) - **⚠️ 必考概念**

这玩意儿不是物理定律，是英特尔创始人戈登·摩尔的一个**预言**。

*   **定义:** 芯片上晶体管 (Transistors) 的数量，大约每 **2年** 翻一番，同时成本减半。（注意：最早说是1年，后来修正为2年，别记错了）。
*   **人话解释:** 电脑性能越来越强，价格越来越便宜。
*   **误区:** 有人说“性能每18个月翻倍”，这是错的！摩尔定律说的是**晶体管数量**。
*   **影响 (Implications):** 性能提升了，以前不敢想的应用（比如登月计算、AI）现在都能实现了。
* 
Each time the cost or performance improves by 10x, new applications emerge.Economically infeasible applications suddenly became practical. (每当成本或性能提高10倍时，就会出现新的应用。经济上不可行的应用突然变得可行。)
---

## 2. 信息的本质 (Information = Bits + Context)

电脑里只有 0 和 1，那它是怎么存文字、图片、视频的？

*   **公式:** **信息 = 位 (Bits) + 上下文 (Context)**
*   **例子:** 同样一串 0 和 1（比如 `01000001`）：
    *   如果上下文是“整数”，它就是数字 `65`。
    *   如果上下文是“文本”，它就是字母 `A`。
    *   如果上下文是“指令”，它可能是一个“加法”动作。
*   **结论:** 只有 0 和 1 是没意义的，必须知道**怎么读**它（上下文）才有意义。

---

## 3. 从代码到程序 (Compilation System) - **⚠️ 流程要背**

你写的 C 语言代码（Hello World），电脑是看不懂的，必须经过翻译。这个翻译过程叫**编译 (Compilation)**。

**四个步骤 (背下来):**
1.  **预处理 (Pre-processor):** 处理 `#include` 这种开头的东西。
2.  **编译 (Compiler):** 把 C 语言翻译成**汇编语言** (Assembly)。
3.  **汇编 (Assembler):** 把汇编语言翻译成**机器语言** (Machine Language/Binary，全是0101)。
4.  **链接 (Linker):** 把你的代码和系统的库（比如 `printf` 的功能）拼在一起，生成最终的可执行文件。

**为什么要懂这个？**
*   优化性能（知道怎么写跑得快）。
*   修 Bug（链接错误 Linker Error 很常见）。
*   安全（防止黑客攻击）。

---

## 4. 硬件核心组件 (Hardware Organization)

电脑跑程序就像人做饭：
1.  **CPU (Processor):** 厨师。负责干活，执行指令。
2.  **主存 (Main Memory):** 案板。存放正在用的食材（数据）和菜谱（代码）。
3.  **总线 (Buses):** 传菜员。负责在 CPU 和内存之间搬运数据。
4.  **I/O 设备:** 冰箱（硬盘）、点单机（键盘）、盘子（显示器）。

**Hello World 是怎么跑起来的？**
1.  **Shell:** 你输入命令 `./hello`。
2.  **Loading:** 操作系统把程序从硬盘（I/O）搬到内存（Memory）。
3.  **Execution:** CPU 从内存里一条条读指令，执行，最后把 "hello, world" 搬到显示器上。
![[deepseek_mermaid_20260107_91ff26.png]]

---

## 5. 缓存 (Caches) - **⚠️ 核心难点**

**问题:** CPU 跑得太快了（像法拉利），内存太慢了（像拖拉机）。CPU 老是要等内存，很浪费时间。
**解决:** 在 CPU 和内存之间加个**缓存 (Cache)**。它容量小但速度极快。

**缓存为什么有用？（两个原理，必考！）**
1.  **时间局部性 (Temporal Locality):**
    *   *人话:* 如果你刚才用了这个数据，你很可能**马上又要用它**。
    *   *例子:* 循环变量 `i`。
2.  **空间局部性 (Spatial Locality):**
    *   *人话:* 如果你用了这个数据，你很可能要用**它隔壁的数据**。
    *   *例子:* 数组，读了 `A[0]` 接着就要读 `A[1]`。

**存储层次结构 (Memory Hierarchy):**
*   寄存器 (最快, 最小) -> L1/L2/L3 缓存 -> 主存 (RAM) -> 硬盘 (最慢, 最大)。
*   **上一层是下一层的缓存。**

---

## 6. 操作系统 (OS) 的三大法宝 (Abstractions) - **⚠️ 默写重点**

操作系统是硬件的大管家，它给程序提供了三个**抽象 (Abstractions)**（也就是三个“假象”），让程序以为自己拥有整个世界。

| 抽象概念 (Abstraction)           | 对应的硬件 (Hardware) | 作用 (Function)            |
| :--------------------------- | :--------------- | :----------------------- |
| **1. 文件 (Files)**            | I/O 设备 (硬盘, 键盘等) | 把所有输入输出设备都看成一串字节，读写方式统一。 |
| **2. 虚拟内存 (Virtual Memory)** | 主存 + 磁盘          | 让程序以为自己独占超级大的内存。         |
| **3. 进程 (Processes)**        | CPU + 内存 + I/O   | 让程序以为自己独占 CPU，实际上是大家轮流用。 |

### 细说进程 (Processes)
*   **上下文切换 (Context Switching):** CPU 在不同进程间快速切换。
    *   *例子:* 你一边听歌一边写文档，CPU 其实是一会儿处理歌，一会儿处理文档，切得太快你感觉不到。
*   **线程 (Threads):** 轻量级的进程。
    *   *比喻:* 进程是**房子**，线程是房子里干活的**人**。一个进程可以有多个线程，它们共享房子里的资源（内存），但干不同的活。

---

## 7. 阿姆达尔定律 (Amdahl’s Law) - **⚠️ 唯一的计算公式 (必考)**

**核心思想:** 想要提升系统总速度，光提升某一部分是不够的，还要看这一部分占总时间的比例。

**公式推导:**

假设老系统的执行时间是 $T_{old} = 1$。
1.  **$\alpha$ (Alpha):** 系统中**可以被优化**的那部分时间比例（比如占总时间的 60%，即 0.6）。
2.  **$k$:** 优化的倍数（比如那部分快了 10 倍）。

新系统的执行时间 $T_{new}$ 由两部分组成：
1.  **不可优化的部分:** $1 - \alpha$ (这部分时间没变)
2.  **被优化的部分:** $\frac{\alpha}{k}$ (这部分时间除以了 k)

$$T_{new} = (1 - \alpha)T_{old} + \frac{\alpha T_{old}}{k} = T_{old}[(1 - \alpha) + \frac{\alpha}{k}]$$

**加速比 (Speedup) 公式:**

$$S = \frac{T_{old}}{T_{new}} = \frac{1}{(1 - \alpha) + \frac{\alpha}{k}}$$

**考试怎么考？**
*   *题目:* 程序的 60% 是可以并行的 ($\alpha = 0.6$)，如果你把这部分加速 10 倍 ($k=10$)，总加速比是多少？
*   *计算:* $S = \frac{1}{(1 - 0.6) + \frac{0.6}{10}} = \frac{1}{0.4 + 0.06} = \frac{1}{0.46} \approx 2.17$
*   *结论:* 哪怕局部加速了 10 倍，整体才快了 2 倍多。**启示：要优化就要优化那些最花时间的部分！**

# 阿姆达尔定律（Amdahl‘s Law）示例

## 定律回顾
**核心思想**：如果一个任务中存在必须串行执行的部分，那么无论将并行部分优化到什么程度，系统的整体加速比都存在理论上限。

**公式**：
$$S = \frac{1}{(1 - p) + \frac{p}{n}}$$
其中：
- \(S\)：理论加速比（相比原始时间）
- \(p\)：可并行部分的时间比例（0 ≤ p ≤ 1）
- \(n\)：处理器（核心）数量
- \(1-p\)：必须串行部分的时间比例

---

## 问题场景
一个视频渲染任务原始总耗时 **20 分钟**：
- **串行部分**（文件读写、资源准备）：**4 分钟**（必须顺序执行）
- **并行部分**（帧渲染计算）：**16 分钟**（可完全并行化）

**计算**：
- 总时间 $T_{\text{old}} = 20$ 分钟
- 串行比例 $1-p = \frac{4}{20} = 0.2$
- 并行比例 $p = \frac{16}{20} = 0.8$

---

## 计算题目与解答

### 1. 使用 4 个核心 (n=4)
**加速比**：
$$
S = \frac{1}{0.2 + \frac{0.8}{4}} = \frac{1}{0.2 + 0.2} = \frac{1}{0.4} = 2.5
$$

**总执行时间**：
$$
T_{\text{new}} = \frac{T_{\text{old}}}{S} = \frac{20}{2.5} = 8 \text{ 分钟}
$$
（验证：串行 4 分钟 + 并行部分 16/4 = 4 分钟 = 8 分钟）

---

### 2. 使用 16 个核心 (n=16)
**加速比**：
$$
S = \frac{1}{0.2 + \frac{0.8}{16}} = \frac{1}{0.2 + 0.05} = \frac{1}{0.25} = 4
$$

**总执行时间**：
$$
T_{\text{new}} = \frac{20}{4} = 5 \text{ 分钟}
$$
（验证：串行 4 分钟 + 并行部分 16/16 = 1 分钟 = 5 分钟）

---

### 3. 使用无限多个核心 (n → ∞)
**最大加速比**：
$$
S_{\text{max}} = \lim_{n \to \infty} \frac{1}{0.2 + \frac{0.8}{n}} = \frac{1}{0.2} = 5
$$

**最小执行时间**：
$$
T_{\text{min}} = \frac{20}{5} = 4 \text{ 分钟}
$$
（即串行部分的时间，此时并行部分时间趋近于 0）

---

## 关键结论
| 核心数 | 加速比 | 总时间 | 说明 |
|--------|--------|--------|------|
| 1      | 1      | 20分钟 | 原始情况 |
| 4      | 2.5    | 8分钟  | 并行化收益明显 |
| 16     | 4      | 5分钟  | 收益递减 |
| ∞      | 5      | 4分钟  | 达到理论上限 |

**重要启示**：
1. **收益递减**：随着核心数增加，加速效果逐渐减弱。
2. **串行瓶颈**：最大加速比由串行比例决定（本例为 $1/0.2 = 5$）。
3. **优化方向**：要大幅提升性能，必须同时优化并行部分**和**串行部分。

 通过这个例子可以直观理解：即使并行部分优化到极致，系统的性能仍受限于串行部分。


## 8. 并发与并行 (Concurrency vs. Parallelism) - **⚠️ 概念辨析**

*   **并发 (Concurrency):**
    *   *定义:* 一个系统同时处理多个任务。
    *   *人话:* **一个人玩杂耍**，手里扔三个球。虽然看起来三个球都在动，但同一时刻手只摸到一个球。
    *   *场景:* 单核 CPU 跑多个程序。
*   **并行 (Parallelism):**
    *   *定义:* 利用并发来让系统跑得更快，真正的同时执行。
    *   *人话:* **三个人玩杂耍**，每个人扔一个球。
    *   *场景:* 多核 CPU，或者超级计算机。

---

## 📝 考前救命小抄 (Summary for Exam)

1.  **Moore's Law:** Transistors double every 2 years. (晶体管2年翻倍)
2.  **Compilation:** Source $\rightarrow$ Preprocessor $\rightarrow$ Compiler $\rightarrow$ Assembler $\rightarrow$ Linker $\rightarrow$ Executable.
3.  **Cache Locality:**
    *   **Temporal:** Use same data again soon. (时间：马上再用)
    *   **Spatial:** Use nearby data soon. (空间：用隔壁的)
4.  **OS Abstractions:**
    *   Files $\rightarrow$ I/O
    *   Virtual Memory $\rightarrow$ Main Memory
    *   Processes $\rightarrow$ CPU + Memory
5.  **Amdahl's Law Formula:** $S = \frac{1}{(1 - \alpha) + \frac{\alpha}{k}}$
6.  **Process vs Thread:** Process = Container (House), Thread = Activity (Person). Threads share memory/code.