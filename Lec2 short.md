# CSC3060 Lecture 2: Data Representation (数据表示)
---

## 1. 基础概念 (Information Storage)

### 1.1 最小单位
*   **Byte (字节)**: 
    *   计算机内存中**最小的可寻址单位**（Smallest addressable unit）。
    *   **1 Byte = 8 bits**。
    *   你可以把它想象成一个小箱子，每个箱子能存8个开关（0或1）。

### 1.2 地址 (Address)
*   **Address (地址)**: 
    *   内存中每个字节都有一个唯一的编号，就像门牌号。
*   **Virtual Address Space (虚拟地址空间)**: 
    *   所有可能地址的集合。简单说就是计算机能画出的最大“地图”。
*   **Pointer (指针)**: 
    *   C语言中的概念。指针 = **地址** + **类型**。
    *   它指向存储块的**第一个字节**的地址。
    *   **类型 (Type)** 决定了计算机怎么读这个地址后面的数据（是读成整数？还是读成小数？）。

---

## 2. 进制转换 (Number Systems) —— ⚠️ 考试计算题重灾区

计算机底层用二进制，但为了人类好读，常用十六进制。

### 2.1 四种进制
| 系统 | 英文 (System) | 基数 (Radix) | 符号 | 例子 |
| :--- | :--- | :--- | :--- | :--- |
| 十进制 | Decimal | 10 | 0-9 | $10$ |
| **二进制** | **Binary** | **2** | **0, 1** | $1010_2$ |
| 八进制 | Octal | 8 | 0-7 | $12_8$ |
| **十六进制** | **Hexadecimal** | **16** | **0-9, A-F** | $A_{16}$ |

> **记忆技巧**：十六进制中，A=10, B=11, C=12, D=13, E=14, F=15。

### 2.2 核心公式与转换

#### ⚠️ 必考1：二进制 $\leftrightarrow$ 十六进制 (Binary to Hex)
这是最常用的，必须秒算！**4个二进制位 = 1个十六进制位**。

*   **方法**：从右往左，每4位一组。
*   **例子**：$1010111001001001_2$ 转 十六进制
    1.  分组：`1010` | `1110` | `0100` | `1001`
    2.  对应：`A` | `E` | `4` | `9`
    3.  结果：`0xAE49`

#### ⚠️ 必考2：十进制 $\rightarrow$ 二进制/十六进制
*   **方法**：**除留余数法** (不断除以2或16，把余数倒着写)。
*   **例子**：把 $47_{10}$ 转二进制
    *   $47 \div 2 = 23$ 余 **1**
    *   $23 \div 2 = 11$ 余 **1**
    *   $11 \div 2 = 5$ 余 **1**
    *   $5 \div 2 = 2$ 余 **1**
    *   $2 \div 2 = 1$ 余 **0**
    *   $1 \div 2 = 0$ 余 **1**
    *   倒着读：$101111_2$

---

## 3. 字长与数据模型 (Word Size & Data Sizes)

### 3.1 字长 (Word Size)
*   指计算机一次能处理的数据宽度，通常也是**指针的大小**。
*   **32-bit 机器**: 指针是 32位 (4字节)，最大内存 $2^{32} \approx 4GB$。
*   **64-bit 机器**: 指针是 64位 (8字节)，最大内存 $2^{64}$ (超级大)。
*   **兼容性**: 64位机器通常能跑32位程序，反之不行。

### 3.2 C语言数据类型大小 (Bytes)
| 类型 (Type) | 32-bit 机器 | 64-bit 机器 | 备注 |
| :--- | :--- | :--- | :--- |
| `char` | 1 | 1 | 永远是1 |
| `short` | 2 | 2 | - |
| `int` | 4 | 4 | 通常是4 |
| `long` | **4** | **8** | ⚠️ 变化点 |
| `pointer` (*) | **4** | **8** | ⚠️ 变化点 |

> **历史小故事 (LP64 vs ILP64)**:
> 为什么 64位机器上 `int` 还是 4字节？
> 为了**数据移植性 (Data Portability)**。如果 `int` 变成8字节，以前存的数据文件读进来就全乱套了。所以现在的标准叫 **LP64** (Long和Pointer是64位，int保持32位)。

---

## 4. 字节序 (Byte Ordering) —— ⚠️ 考试超级大坑

当一个数据跨越多个字节（比如 `int` 占4字节）时，这就涉及顺序问题。

假设有一个整数：`0x01234567` (十六进制)
*   最高有效字节 (Most Significant Byte, MSB): `01`
*   最低有效字节 (Least Significant Byte, LSB): `67`

### 4.1 大端 vs 小端
1.  **大端模式 (Big Endian)**:
    *   **符合人类阅读习惯**。最高位存放在最低地址。
    *   内存样子：`01` `23` `45` `67`
    *   使用阵营：网络协议 (TCP/IP)、IBM。
2.  **小端模式 (Little Endian)**:
    *   **反着来**。最低位存放在最低地址。
    *   内存样子：`67` `45` `23` `01`
    *   使用阵营：**Intel x86**, 大部分个人电脑。

> **⚠️ 考试题型**：
> 题目：在小端机器上，`int x = 0x12345678`，地址是 `0x100`。问 `0x100` 地址里存的是什么？
> 答案：`78` (因为是小端，屁股先入库)。

---

## 5. 位运算与逻辑运算 (Operations)

### 5.1 位运算 (Bitwise Operations)
对每一位单独操作。
*   `&` (AND): 全1则1。
*   `|` (OR): 有1则1。
*   `~` (NOT): 0变1，1变0。
*   `^` (XOR): **相同为0，不同为1**。

> **经典算法：不使用临时变量交换两个数 (XOR Swap)**
> ```c
> *x = *x ^ *y;
> *y = *x ^ *y;
> *x = *x ^ *y;
> ```
> 原理：利用 $a \oplus a = 0$ 的特性。

### 5.2 逻辑运算 (Logical Operations)
结果只有 **True (1)** 或 **False (0)**。
*   `&&` (Logic AND): 逻辑与。
*   `||` (Logic OR): 逻辑或。
*   `!` (Logic NOT): 逻辑非。
*   **短路特性 (Short-circuit)**:
    *   `A && B`: 如果 A 是假，B 根本不会被执行。
    *   `A || B`: 如果 A 是真，B 根本不会被执行。

> **⚠️ 陷阱**: `if (A & B)` 和 `if (A && B)` 是完全不同的！
> `2 & 1` 结果是 0 (False)，但 `2 && 1` 结果是 1 (True)。

### 5.3 移位运算 (Shift Operations)
*   **左移 (`<<`)**: 丢弃高位，**右边补0**。 ($x << k$ 相当于 $x \times 2^k$)
*   **右移 (`>>`)**: 
    *   **逻辑右移 (Logical)**: 左边**补0**。(用于无符号数)
    *   **算术右移 (Arithmetic)**: 左边**补符号位**。(用于有符号数，保持正负号不变)

---

## 6. 整数表示 (Integer Representation) —— ⚠️ 最难理解的部分

### 6.1 无符号数 (Unsigned)
*   没有负数，全是正数。
*   公式：$B2U(X) = \sum_{i=0}^{w-1} x_i \cdot 2^i$

### 6.2 有符号数 (Signed) - 补码 (2's Complement)
现代计算机**几乎全部**使用**补码**来表示负数。

*   **最高位 (MSB)** 是符号位：**0代表正，1代表负**。
*   **正数**: 和无符号数一样。
*   **负数**: 
    *   **公式**: $B2T(X) = -x_{w-1} \cdot 2^{w-1} + \sum_{i=0}^{w-2} x_i \cdot 2^i$
    *   **人话转换法**:
        *   **原码转补码**: 符号位不变，其余位**取反**，最后**加1**。
        *   例子 (-2): 
            1. +2 原码: `0010`
            2. 取反: `1101`
            3. 加1: `1110` (这就是-2的补码)

### 6.3 为什么要用补码？
1.  **唯一的0**: 原码和反码都有 +0 和 -0，补码只有全0。
2.  **加法简单**: 计算机算 `5 - 3` 可以直接算 `5 + (-3)`，电路设计简单，不需要减法器。

### 6.4 ⚠️ 必背范围公式 (n位补码)
*   **最大正数 (TMax)**: $2^{n-1} - 1$ (符号位0，其他全1，如 `0111`)
*   **最小负数 (TMin)**: $-2^{n-1}$ (符号位1，其他全0，如 `1000`)
*   **-1 的补码**: 全是1 (`111...111`)。

### 6.5 符号扩展 (Sign Extension)
当把一个小类型（如 `char`）转成大类型（如 `int`）时：
*   **无符号数**: 前面补 0。
*   **有符号数**: 前面**补符号位** (如果原来是负数，前面全补1；原来是正数，全补0)。
*   *目的*: 保持数值大小不变。

---

## 7. 总结：考试必背 Cheat Sheet

1.  **1 Byte = 8 bits**。
2.  **Hex转Bin**: 1位Hex = 4位Bin。
3.  **小端 (Little Endian)**: 低位在低地址 (反直觉)。
4.  **补码求负数**: **取反 + 1**。
5.  **TMin (最小负数)**: `100...00` ($-2^{n-1}$)。
6.  **TMax (最大正数)**: `011...11` ($2^{n-1}-1$)。
7.  **-1 的补码**: `111...11` (`0xFF...FF`)。
8.  **算术右移**: 补符号位；**逻辑右移**: 补0。
9.  **XOR Swap**: `a=a^b; b=a^b; a=a^b;`