# 整数算术（Integer Arithmetic）总结

## 1. 二进制加减法与溢出（Binary Addition/Subtraction & Overflow）

计算机用固定数量的比特（比如8位、32位）来存数字。位数是有限的，所以算出来的数太大就可能“装不下”，这就是**溢出（Overflow）**。

### 1.1 补码（Two‘s Complement）
- **是什么**：计算机表示**负数**的一种方法。好处是**加法**和**减法**可以用**同一个电路**（加法器）来做。
- **怎么算**：
    - 一个数 `B` 的补码（也就是 `-B`） = **把 B 的每个比特取反（0变1，1变0），然后加1**。
    - 公式：`-B = ~B + 1` （`~` 表示按位取反）。
- **例子**：用8位二进制。
    - `6` 是 `00000110`。
    - `-6` 就是：
        1. 取反：`11111001`
        2. 加1：`11111010`
    - 所以 `7 - 6 = 7 + (-6)`：
        `00000111 + 11111010 = (1)00000001`。
        最前面那个1（第9位）装不下了，被丢掉，结果就是 `00000001`，也就是1。正确！

### 1.2 溢出的样子（What Overflow Looks Like）
- **例子1**（有符号数，8位）：
    - `127 + 3`：`01111111 + 00000011 = 10000010`。
    - 两个**正数**（首位是0）相加，结果变成了**负数**（首位是1）。这不对！这就是溢出。
- **例子2**：
    - `-128 + (-6)`：`10000000 + 11111010 = (1)01111010`。
    - 两个**负数**（首位是1）相加，结果变成了**正数**（首位是0）。这也不对！也是溢出。
    - 从最高位（MSB）产生的进位（那个1）被丢掉了。

### 1.3 为什么要管溢出？（Why Care?）
溢出的结果是错的，用错了会出大事（比如火箭爆炸、钱算错）。
但很多系统（如C语言）默认**不报错**，因为：
1.  报错检查拖慢速度（**性能**）。
2.  历史原因（老硬件简单）。
3.  有些人就想要“溢出后绕回”的效果（比如做哈希、加密）。

**怎么办？** 不同语言有不同策略：
- **Python**：数字位数可以动态变长，不怕溢出。
- **Rust/C#**：可以用 `checked_add` 这类函数，溢出就报异常。
- **饱和运算（Saturation）**：算到最大/最小值就停住，不再变化。多媒体指令（MMX/SSE）常用。

## 2. 溢出检测（Overflow Detection）

### 2.1 有符号数（Signed Numbers）的溢出检测
**核心规律**：如果**两个加数的符号相同**，但**结果的符号和它们不同**，那就溢出了。

我们可以列个表来记（考试爱考）：

| 运算 | 操作数A | 操作数B | 结果显示出溢出 |
| :--- | :--- | :--- | :--- |
| A + B | >=0 (正) | >=0 (正) | <0 (负) |
| A + B | <0 (负) | <0 (负) | >=0 (正) |
| A - B | >=0 (正) | <0 (负) | <0 (负) |
| A - B | <0 (负) | >=0 (正) | >=0 (正) |

**用逻辑判断**（对于A+B）：
`溢出 = (非(符号A异或符号B)) 与 (符号A异或结果符号)`
解释：“符号相同”就是 `非(符号A异或符号B)` 为真。“结果符号不同”就是 `(符号A异或结果符号)` 为真。两个同时为真，就溢出。

### 2.2 无符号数（Unsigned Numbers）的溢出检测
这个简单，用 if 语句（分支）判断就行：
- **加法**：如果 `和 < 加数A` **或** `和 < 加数B`，那就溢出了。
    - 公式：`c = a + b`，则 `溢出 = (c < a)` （判断一个就行，因为无符号数溢出后肯定变小）。
- **减法**：如果 `差 > 被减数`，那就溢出了（实际上，无符号数减法不会向上溢出，只会“下溢”，即借位）。

## 3. 算术逻辑单元（ALU）怎么工作

### 3.1 1位ALU（1-bit ALU）
像一个微型计算器，能处理两个1比特的数（A和B）。
- **功能**：根据控制信号，选择做**与（AND）**、**或（OR）**、**加法（ADD）**。
- **关键部件**：
    1.  **一个全加器（Full Adder）**：计算本位和（Sum）和进位输出（C~out~）。
    2.  **一个多路选择器（Multiplexer）**：像开关一样，选择输出哪种运算结果。

**全加器逻辑**：
- 本位和 $S_i = A_i \oplus B_i \oplus C_{in}$ （$\oplus$ 是异或）。
- 进位输出 $C_{out} = (A_i \& B_i) | (C_{in} \& (A_i \oplus B_i))$。
    - 产生进位的三种情况：
        1.  A和B都是1。
        2.  A是1，B是0，且进位输入是1。
        3.  A是0，B是1，且进位输入是1。

### 3.2 用1位ALU拼成多位ALU
把多个1位ALU串起来，低位的进位输出（C~out~）接到高位的进位输入（C~in~），就变成了能算多位二进制数的加法器。

### 3.3 实现减法（Implementing Subtraction）
**秘诀**：$A - B = A + (-B) = A + (\sim B + 1)$。
- 在ALU中：
    1.  将输入B的每一位取反（通过一个控制信号）。
    2.  将最低位ALU的进位输入（C~in~）设为1。
    3.  然后做加法。这样电路就实现了减法！

### 3.4 实现“小于则置位”（Set Less Than, SLT）
比较A是否小于B。
- **做法**：计算 $A - B$，然后**只看结果的符号位**。
    - 如果 $A - B$ 是负数（符号位为1），说明 A < B，那么SLT输出1。
    - 否则输出0。
- 在ALU里，需要把最高位（符号位）引出来，作为SLT的结果。

## 4. 更快的加法器：超前进位（Carry Lookahead）

### 4.1 问题：串行进位（Ripple Carry）太慢
- 在普通的加法器里，进位像波浪一样，从最低位一位一位传到最高位。
- 对于64位加法，要等64个进位传递延迟，太慢了！

### 4.2 解决方案：提前算好所有进位
**定义两个信号**（对于第i位）：
- **生成（Generate）** $g_i = A_i \& B_i$。
    - 如果g~i~=1，说明这一位**肯定**会产生一个进位（不管有没有进位输入）。
- **传播（Propagate）** $p_i = A_i \oplus B_i$。
    - 如果p~i~=1，说明这一位会把进位输入**传递**到进位输出（如果A和B相同，就不会传递）。

**用g和p提前计算进位**：
- 第1位的进位：$C_1 = g_0 + (p_0 \& C_0)$
- 第2位的进位：$C_2 = g_1 + (p_1 \& g_0) + (p_1 \& p_0 \& C_0)$
- 第3位的进位：$C_3 = g_2 + (p_2 \& g_1) + (p_2 \& p_1 \& g_0) + (p_2 \& p_1 \& p_0 \& C_0)$
- ... 以此类推。

**好处**：所有位的进位都可以**并行**计算，不用等前面的结果，速度飞快！
**代价**：电路变复杂了，特别是位数多的时候。实际中会分层组（如4位一组）来平衡速度和复杂度。

## 5. 乘法（Multiplication）

### 5.1 基本思路：移位和相加
乘法可以分解成一系列“被乘数左移”和“相加”的操作。
- 看乘数的每一位：
    - 如果该位是1，就把**被乘数**（根据当前是第几位，左移对应位数后）加到结果上。
    - 如果该位是0，就加0（相当于不加）。

### 5.2 乘法硬件（简单版）
需要一个**被乘数寄存器**、一个**乘积寄存器**（初始放乘数，并用来存最终结果的高位）、一个**ALU**。
- **步骤**：
    1.  检查乘数寄存器的最低位。
    2.  如果是1，就把被乘数加到乘积寄存器的高位部分。
    3.  把整个乘积寄存器**右移**一位（这样就把乘数寄存器的最低位挤掉，同时把新的结果位移进来）。
    4.  重复32次（对于32位数）。

### 5.3 有符号数乘法（Signed Multiplication）
1.  把被乘数和乘数都变成正数，记住它们原来的符号。
2.  像无符号数一样相乘。
3.  最后看符号：如果**原来的两个数符号不同**（一正一负），就把最终结果取负（求补码）。

**重要事实**：在现代计算机（使用补码标准后），你可以直接把两个有符号数当成无符号数的位模式去乘，结果的**低N位**（N是位数）对于有符号乘法来说就是正确的！硬件设计简单了。

### 5.4 用移位和加法代替乘法（编译器优化）
编译器很聪明，如果乘以一个**常数**，它会把这个常数拆成2的幂次的和，然后用移位和加法来代替乘法，因为移位和加法比乘法器快。
- **例子**：
    - $x * 13 = x * (8 + 4 + 1) = (x << 3) + (x << 2) + x$
    - $x * 1054 = x * (1024 + 32 - 2) = (x << 10) + (x << 5) - (x << 1)$

### 5.5 快速乘法器：用空间换时间（Fast Multiplier: Trading Area for Speed）

前面说的乘法器（第5.2节）是**串行**工作的，就像工厂的一条流水线，一次只处理一位乘数，需要很多个时钟周期才能算完（比如32位数要32个周期）。

**怎么能更快？**
- **核心思想**：既然乘数的每一位（0或1）决定了要不要加“被乘数”，那我们**可以提前把所有位要加的东西（称为“部分积”）一次性都准备好**，然后用一堆加法器同时把它们加起来！
- **这就叫“并行”计算**。

**具体怎么做？**
1.  **生成部分积**：对于乘数的第0位、第1位、第2位……直到第31位，同时判断：
    - 如果该位是 `1`，则生成 `(被乘数 << 位数)` 作为一个部分积。
    - 如果该位是 `0`，则生成 `0` 作为一个部分积。
    - 这样，一个32位乘法就会瞬间得到32个“部分积”。

2.  **加起所有部分积**：现在的问题变成了“如何快速地把这32个数相加”。
    - 最简单但最慢的方法：用一个加法器，把这32个数一个接一个地加进去，需要31次加法。
    - **快速的方法**：用**加法器树**（像金字塔一样）。
        - 第一层：用16个加法器，把32个数两两相加，得到16个结果。
        - 第二层：用8个加法器，把这16个结果两两相加，得到8个结果。
        - 依此类推……经过 $log_2(32) = 5$ 层，就能得到最终结果！
    - 这样，**延迟从31次加法缩短到了5次加法**，速度大大提升。

**代价是什么？**
- **用更多的晶体管换速度**：并行乘法器需要很多个加法器同时工作，比串行乘法器占用芯片面积大得多（更“费电”、更“贵”）。
- 这就是计算机设计中经典的 **“面积-速度折衷”**。随着摩尔定律发展，晶体管越来越便宜、越来越小，所以我们愿意用更多的面积来换取计算速度的飞跃。

**一句话总结**：快速乘法器就是“人多力量大”——雇一大堆加法器小弟同时干活，当然比一个加法器老黄牛自己干要快得多！
## 6. 除法（Division）

### 6.1 基本思路：反复试减和移位
和十进制除法类似：看被除数（或当前余数）能减几次除数。
- **步骤**：
    1.  把除数左移到和被除数高位对齐。
    2.  尝试用当前余数减除数。
        - 如果够减（结果非负），商对应位置1，余数更新为减的结果。
        - 如果不够减（结果为负），商对应位置0，余数不变。
    3.  把除数右移一位。
    4.  重复，直到除数移回原位。

### 6.2 除法硬件
需要：64位除数寄存器（存左移后的除数）、64位ALU、64位余数寄存器（初始放被除数）、32位商寄存器。
- 流程就是上述“试减-移位”的硬件实现。

### 6.3 有符号数除法（Signed Division）
核心规则：**被除数（Dividend）和余数（Remainder）必须同号**。
为了避免负数除法下出现歧义，比如这种：$-7/2 = 3 ~~reminder ~~-1~~~or~~~-7/2 = 4~~reminder +1$ 
- **步骤**：
    1.  记住被除数和除数的原始符号。
    2.  把它们都当正数来处理，做除法。
    3.  调整结果：
        - **商（Quotient）**：如果被除数和除数符号不同，则商为负。
        - **余数（Remainder）**：余数的符号必须和被除数相同。如果不同，需要调整（给余数加上除数，同时商减1）。

**公式**：$被除数 = 商 \times 除数 + 余数$

### 6.4 除以2的幂（用移位实现）
- **正数**：直接算术右移（SRA）。$X / 2^k = X >> k$。
- **负数**：不能直接右移！因为直接右移是向下取整，而我们需要的是向零取整。
    - **正确方法**：$(X + (2^k - 1)) >> k$ （仅当X为负数时需要加这个偏置）。
    - **例子**：$-7 / 2$
        - 错误：`11111001 >> 1 = 11111100` (这是-4，向下取整了)。
        - 正确：$(-7 + (2-1)) >> 1 = (-7+1)>>1 = 11111010>>1 = 11111101$ (这是-3，向零取整)。

## 7. 体系结构层面的思考（Architectural Considerations）

### 7.1 硬件检测溢出的开销（Overhead of Hardware Overflow Trap）
- **性能影响**：检查溢出需要额外的电路和判断，会拖慢所有运算，即使大部分运算并不溢出。
- **处理陷阱（Trap）很昂贵**：要保存现场、跳转到处理程序、再恢复，严重影响流水线和乱序执行。
- **设计复杂**：要实现**精确异常（Precise Exception）**，要求发生异常的指令之前的所有指令都已完成，之后的指令都未开始。这在乱序执行（Out-of-Order Execution）的CPU中很难高效实现。

### 7.2 RISC-V 的设计哲学
RISC-V选择**把溢出检测的责任交给软件**。
- **整数运算**：硬件不主动触发溢出异常。如果你需要检测，编译器要生成额外的检查指令（如用比较分支指令）。
- **浮点运算**：硬件会设置状态标志（fflags），但**不会自动触发异常**。软件需要主动去读这个标志来判断。
- **好处**：简化了硬件设计，提高了主流情况（不溢出）下的性能。把选择权交给了编程语言和程序员。
- 其他指令集（如MIPS, ARM）通常提供能触发硬件陷阱的指令，而RISC-V把这部分放到了可选的扩展中。

### 7.3 精确异常（Precise Exception）
- **定义**：当异常发生时，处理器的状态必须是“干净的”，程序能够从**恰好引起异常的那条指令**处恢复执行，就像异常从未发生过一样。
- **为什么难**：在乱序执行CPU中，后面的指令可能先执行完。如果前面的指令发生异常，这些已经更新的状态（寄存器、内存）需要能“撤销”或保证不被外部看到，这需要复杂的中断和重排序缓冲机制。