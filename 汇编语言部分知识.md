

## RISC-V 中的汇编指令
### 1.ALU Instructions(算术逻辑单元指令)

| 指令   | 简单解释                          |
| ---- | ----------------------------- |
| ADD  | 寄存器加法：rd = rs1 + rs2          |
| SUB  | 寄存器减法：rd = rs1 - rs2          |
| AND  | 按位与：rd = rs1 & rs2            |
| OR   | 按位或：rd = rs1 \| rs2           |
| XOR  | 按位异或：rd = rs1 ^ rs2           |
| SLT  | 有符号比较，小于则 rd=1，否则 rd=0        |
| SLTU | 无符号比较，小于则 rd=1，否则 rd=0        |
| SLL  | 逻辑左移：rd = rs1 << (rs2 的低 5 位) |
| SRL  | 逻辑右移，高位补 0                    |
| SRA  | 算术右移，高位补符号位                   |
### 2.ALU Inst with immed 

| 指令    | 简单解释                       |
| ----- | -------------------------- |
| ADDI  | 立即数加法：rd = rs1 + imm       |
| ANDI  | 按位与立即数：rd = rs1 & imm      |
| ORI   | 按位或立即数：rd = rs1 \| imm     |
| XORI  | 按位异或立即数：rd = rs1 ^ imm     |
| SLTI  | 有符号与立即数比较，小于则 rd=1，否则 rd=0 |
| SLTIU | 无符号与立即数比较，小于则 rd=1，否则 rd=0 |
| SLLI  | 逻辑左移 imm 位：rd = rs1 << imm |
| SRLI  | 逻辑右移 imm 位，高位补 0           |
| SRAI  | 算术右移 imm 位，高位补符号位          |
### 3.LOAD/STORE(加载/存储)

| 指令  | 简单解释                                                          |     |
| --- | ------------------------------------------------------------- | --- |
| LB  | 从内存按字节加载到寄存器，有符号扩展为 32 位                                      |     |
| LH  | 从内存按半字（16 位）加载，有符号扩展为 32 位                                    |     |
| LW  | 从内存按字（32 位）加载                                                 |     |
| LBU | 按字节加载到寄存器，无符号零扩展为 32 位                                        |     |
| LHU | 按半字加载到寄存器，无符号零扩展为 32 位                                        |     |
| SB  | 将寄存器最低 8 位存入内存（1 字节）                                          |     |
| SH  | 将寄存器最低 16 位存入内存（2 字节）                                         |     |
| SW  | 将寄存器 32 位内容存入内存（4 字节）                                         |     |
| LI  | 将一个常数（立即数）存入寄存器中，这是一条伪指令，加载的数字大小和CPU直接相关，硬件会自动将这条指令拆解成合适的汇编指令 |     |
| LD  | 加载双字(8字节),但是只在 RV64I 才有                                       |     |
|     |                                                               |     |
### 4.Branch(分支/条件跳转)

差不多可以理解为
~~~cpp
if(condition) goto 
else goto 
~~~

| 指令   | 简单解释                                       |
| ---- | ------------------------------------------ |
| BEQ  | 若 rs1 == rs2 则跳转到目标标签                      |
| BNE  | 若 rs1 != rs2 则跳转到目标标签                      |
| BLT  | 有符号比较，若 rs1 <  rs2 则跳转                     |
| BGE  | 有符号比较，若 rs1 >= rs2 则跳转                     |
| BLTU | 无符号比较，若 rs1 <  rs2 则跳转                     |
| BGEU | 无符号比较，若 rs1 >= rs2 则跳转                     |
| BNEZ | 比较当前寄存器是不是0,比 BNE 少一个参数,<br>可以认为是一个特殊的 BNE |
### 5.Jump(无条件跳转)
比如
~~~cpp
a = a + 1
goto ... (无条件跳转到别的地方)
~~~
这俩的区别有点神秘，插个眼在这里先

| 指令   | 简单解释                                                                                            |
| ---- | ----------------------------------------------------------------------------------------------- |
| JAL  | Jump And Link：跳转到目标地址，并把返回地址写入 rd（常用 ra）<br>比如你一个函数执行完之后想要跳转到的地址就可以存入ra，ret 指令会跳回来这里,范围是 20位立即数 |
| JALR | 目标地址在寄存器中的跳转，并把返回地址写入 rd，范围就是RISCV的上限 32 位                                                      |
| J    | 简化版的 JAL，J label <-> JAL x0 label，就是相当于省略了一个返回地址 x0                                             |
| JR   | 跳转到某个寄存器中所保存的绝对地址去执行代码，也是一条伪指令                                                                  |
### 6.Upper(装载高位立即数)
| 指令    | 简单解释                                             |
| ----- | ------------------------------------------------ |
| LUI   | Load Upper Immediate：将 20 位立即数装入寄存器高 20 位，低位清零   |
| AUIPC | Add Upper Immediate to PC：PC + 高 20 位立即数，结果写入寄存器 |
