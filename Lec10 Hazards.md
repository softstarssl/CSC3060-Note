# CSC3060 计算机体系结构 — 第4章：流水线冒险（Pipeline Hazards）

## 一、总体概念：流水线为什么会出问题？

想象一条工厂流水线，正常情况下每个工人同时在处理不同的零件，效率极高。但如果前面的工人没干完，后面的工人就不得不停下来等——这就是"冒险（Hazard）"。

流水线的**理论加速比** = 流水线级数（比如5级就快5倍）。但现实中做不到，原因有三大冒险：

| 冒险类型 | 英文                | 一句话解释                |
| ---- | ----------------- | -------------------- |
| 结构冒险 | Structural Hazard | 两条指令抢同一个硬件资源         |
| 数据冒险 | Data Hazard       | 后面的指令需要前面指令还没算完的数据   |
| 控制冒险 | Control Hazard    | 遇到分支跳转，不知道下一条该执行哪条指令 |
|      |                   |                      |

> ⚠️ 冒险通常会让指令在 **ID阶段（指令译码阶段）被阻塞（Stall）**，产生"流水线气泡（Pipeline Bubble）"，浪费时钟周期。

## 二、流水线的填充与排空（Fill & Drain）

- **填充（Fill）**：流水线刚启动时，各级逐渐被指令填满的阶段
- **满载（Pipe Full）**：所有流水线级都在工作，效率最高
- **排空（Drain）**：最后一批指令逐渐走完流水线各级的阶段

即使没有任何冒险，填充和排空阶段本身就会降低效率，所以实际加速比 < 流水线级数。
## 三、结构冒险（Structural Hazard）⭐

### 3.1 定义
由于**硬件资源不足(hardware resource limitations)**，两条（或多条）指令同时想用同一个硬件单元，导致冲突。

理论上来说，如果有**无限的**硬件资源(晶体管,**transistors**)，这个问题是不会存在的
### 3.2 典型例子

**例1：冯·诺依曼架构的内存冲突**
- 一条指令在 MEM 阶段访问**数据内存**
- 同时另一条新指令在 IF 阶段想取**指令**
- 冯·诺依曼架构指令和数据共用同一块内存 → **冲突！**

**例2：除法单元冲突**
- 两条除法指令相邻，每条除法占用功能部件 32~64 个时钟周期
- 第二条必须等第一条释放除法单元
### 3.3 为什么标量架构结构(scalar architectures)冒险较少？

标量架构：每个周期只会发射1条指令(也就是说每个周期只有**一**条新的指令进入pipeline),所以硬件资源消耗少

超标量架构(**superscalar architectures**):每个时钟周期可以同时发射**多条指令**，需要多套执行单元（多个 ALU、多个 load/store 单元等）并行工作,现代的 CPU 基本都是这个

- 寄存器有**足够多的读写端口**
- 大多数功能单元要么单周期完成，要么本身已流水线化（除法除外）
- 使用**哈佛架构（Harvard Architecture）**：**指令缓存（I-cache）和数据缓存（D-cache）分离**
- 需要**两个加法器**：一个计算 PC+4，一个做 ALU 运算

> 📌 **哈佛架构**之名来源于哈佛大学的 Mark I/II/III 计算机，最初是技术限制导致的分离，不是主动的架构设计选择。
### 3.4 解决方案

| 方案                                | 说明                                                                                        |
| --------------------------------- | ----------------------------------------------------------------------------------------- |
| **流水线停顿（Pipeline Stall）**         | 硬件检测到冲突，让新指令在 ID 阶段等待，插入气泡                                                                |
| **增加资源（Multiple Resources）**      | 如分离 I-cache 和 D-cache；增加多个乘除法单元                                                           |
| **指令重排序（Instruction Reordering）** | 编译器静态调度（Static Scheduling）或硬件动态调度（Dynamic Scheduling）,比如把不相关的指令“塞”进那些原本因为资源冲突而不得不停顿的时钟周期里 |
| **ISA 设计约束**                      | 例如禁止 ALU 操作直接访问内存操作数，减少寄存器写端口需求                                                           |
|                                   |                                                                                           |

## 四、数据冒险（Data Hazard）⭐⭐

### 4.1 定义

后面的指令需要读取某个寄存器的值，但该值还没被前面的指令写回。
记住，Project3 的 code scheduling 基本就是用来解决 data hazard 的

### 4.2 经典例子（一定要记住！）

```
Add X9,  X0, X1    // 第1条：X9 的新值在 EXE 阶段末才计算出来，WB 阶段才写回
Sub X10, X9, X3    // 第2条：ID 阶段需要读 X9，但第1条还没写回！
```

**最坏情况：第2条要等第1条 WB 完才能在 ID 读取 → 浪费3个时钟周期（3个气泡）**

### 4.3 三种子情形

#### 情形1：寄存器访问问题（Register Access Issue）

- 同一个周期内，一条指令在 WB 阶段写寄存器，另一条在 ID 阶段读同一寄存器
- 解决方法：**写后读（Write-then-Read）**，在同一个周期内先写后读,前半周期写，后半周期读（寄存器文件设计支持, 这玩意有一个输入端口和输出端口）

#### 情形2：ALU 结果访问问题（ALU Result Access Issue）

- EXE 阶段计算结果还没写回，但下一条/下下条指令就需要用了
- 最经典的数据相关问题

#### 情形3：Load 数据冒险（Load-Use Hazard）

- Load 指令在 **MEM 阶段**才能拿到数据（比 ALU 结果晚一级）
- 即使用了转发，下一条紧跟的指令也没法在同一周期拿到这个值

### 4.4 解决方案 1：流水线停顿（Pipeline Stall）⭐

让需要数据的指令在 **ID 阶段等待**，直到数据可用。

- ALU 数据冒险：等 **2 个周期**（插入2个气泡）
- Load 数据冒险：等 **1 个周期**（Load-Use 冒险，插入1个气泡）

**缺点**：浪费时钟周期，降低性能。

### 4.5 解决方案 2：数据转发/旁路（Data Forwarding / Bypassing）⭐⭐

**核心思想**：不等写回寄存器，直接把计算结果从流水线寄存器"短路"传给需要数据的阶段。

```
         IF  ID  EX  MEM  WB
Add X9:          ←结果在这里→
Sub X10:     IF  ID  [需要X9]
                      ↑
                 EX/MEM 流水线寄存器直接转发过来！
```

#### ⭐ 转发检测条件（Hazard Detection Conditions）— 考试必考！

检测是否需要从 **EX/MEM 流水线寄存器**转发（1a / 1b 条件）：

```
1a. EX/MEM.RegRd == ID/EX.RegRs1 
1b. EX/MEM.RegRd == ID/EX.RegRs2

相当于前一条指令的目标寄存器是当前指令的两个源寄存器之一
```

检测是否需要从 **MEM/WB 流水线寄存器**转发（2a / 2b 条件）：

```
2a. MEM/WB.RegRd == ID/EX.RegRs1
2b. MEM/WB.RegRd == ID/EX.RegRs2

和上面那个意思差不多，只是这次两条相关指令中间应该是还有一条指令，而不是相邻的，前面那个是相邻
```

#### ⭐ 转发的特殊情况（Corner Cases）— 考试容易考！

1. **如果指令不写寄存器（RegWr 未置位）→ 不转发**
    - 例如 Store 指令不写目标寄存器,分支跳转指令也没有
    - 可能这个位置是立即数，刚好和后面指令的源寄存器相同，但是显然我们不需要去转发
2. **如果目标寄存器是 X0（零寄存器）→ 不转发**
    - X0 永远是 0，写 X0 没有意义
    - 检查：`RegRd ≠ 0`

#### Load-Use 冒险：转发也救不了的情况

```
         IF  ID  EX  MEM  WB
ld  X9:              ← 数据在 MEM 结束才有
add X10:     IF  ID  EX  ← EX 需要 X9，但 MEM 还没跑完！
```

在这个例子中，新的指令要在第四个时钟周期开始就要那个数据，但是 ld 指令得在第四个时钟周期末尾才能拿到指令，这个时间差在三维空间中好像还无法解决吧 

**结论：Load 后紧跟使用该寄存器的指令，必须停顿1个周期（插入1个气泡），然后再用转发。**

### 4.6 解决方案 3：编译器代码调度（Compiler Code Scheduling）

编译器重排指令，把无关指令填到"生产-使用间隔"中，避免冒险。

- **MIPS** 早期实现就没有硬件互锁，完全依赖编译器
- 如果找不到可以填的指令，就插入 **NOP（空操作指令）**
- 大多数现代处理器都有硬件互锁，编译器调度是进一步优化

**编译器调度的局限性：**

- 内存依赖（Memory Dependence）通常在编译时未知
- 分支、间接跳转、函数调用/返回 会限制调度范围

### 指令调度 (Code Scheduling) 核心挑战总结

#### 1. 内存依赖的现实困境 (Memory Dependency)
* **别名问题 (Aliasing)：** 编译器难以确定两个内存指令是否指向同一地址，为保正确必须**保守 (Conservative)**，限制重排。
* **不确定性：** 内存延迟 (Latency) 随 Cache 命中情况剧烈波动，静态调度难以精准预估。
* **风险控制：** 投机性移动 (Speculative) 内存指令可能触发非法访问 (Trap)，导致程序崩溃。

#### 2. 搜索空间的组合爆炸 (Legal Schedule)
* **数学本质：** 合法调度是符合 DAG 约束的**拓扑排序 (Topological Sort)**。
* **计算复杂性：** 随指令数 $N$ 增加，合法排列数呈指数级增长。当 $N = 50$ 时，排列数高达 $3 \times 10^{64}$，就是 $N!$。
* **结论：** 寻找全局最优解是 **NP-complete** 问题，实际应用中编译器通常采用**启发式算法 (Heuristics)** 寻找较优解。

这里很多都是 Project3 中涉及的

## 五、控制冒险（Control Hazard）⭐⭐

### 5.1 定义

遇到**分支指令（Branch）** 或 **JALR** 时，流水线还不知道"接下来该执行哪条指令"，如果猜错了就要作废已经取进来的指令。

> 📌 **为什么严重？** 分支指令占所有指令的 **15%~30%**，大约每 4~5 条就有 1 条分支！

### 5.2 分支结果何时可知？

- 原始设计：分支条件在 **MEM 阶段** 才知道 → 要作废 **3 条**错误取进来的指令
- 优化后：分支条件在 **EX 阶段** 可知 → 要作废 **2 条**

### 5.3 ⭐ 控制冒险性能损失公式（必考！）

$$\text{CPI 惩罚} = \text{被冲刷指令数} \times P(\text{分支}) \times P(\text{跳转})$$
P(分支) = 分支占所有指令数量的比例
P(跳转) = 每个分支指令跳转的概率

**例子1（MEM 阶段判断，冲刷3条）：** $$\text{CPI 惩罚} = 3 \times 0.2 \times 0.5 = 0.3$$ 即性能下降 **30%**

**例子2（EX 阶段判断，冲刷2条）：** $$\text{CPI 惩罚} = 2 \times 0.2 \times 0.5 = 0.2$$ 即性能下降 **20%**

> 假设：20% 的指令是分支，50% 的分支会跳转，基础 CPI = 1

### 5.4 控制冒险解决方案概览

|方案|核心思想|精度|
|---|---|---|
|刷新流水线 (Flush)|冲刷错误取入的指令，安全但慢|100%（无预测）|
|静态分支预测|编译时决定预测方向|65%~85%|
|动态分支预测|运行时根据历史决定|90%~99%|


## 六、静态分支预测（Static Branch Prediction）⭐

在**编译时**确定预测结果，不随程序运行变化。
### 常见策略：

1. **预测"不跳转"（Predict Not Taken）**：猜下一条就是 PC+4
2. 为什么要预测不跳转？ 因为预测跳转你也得等到 ID 之后你才能计算出跳转地址，你还是要停下来，但是 PC+4 在IF 阶段就会被算出来了，运气好你就没有流水线气泡了。这两者的差异至少有一个时钟周期
3. **向后分支预测跳转**（Backward branch → predict Taken）：循环回跳通常会跳，这玩意主要对付循环
4. **向前分支预测不跳转**（Forward branch → predict Fall-through）：if 语句通常不跳
5. **ISA 预测位**：指令中保留一位作为预测提示
6. **寄存器顺序提示**（如 HP 的方案）：`beq r3,r4,8` → 不跳；`beq r4,r3,8` → 跳

显然，这个方法太粗暴了，CPI 惩罚比例大的让人难以接受

## 七、动态分支预测（Dynamic Branch Prediction）⭐⭐

运行时根据历史记录进行预测，精度远高于静态(60%左右)（动态可达 90%~99%）。
### 7.1 分支历史表（Branch History Table, BHT）⭐

- 用**最近分支指令地址（PC的低几位）** 作为索引
- 存储该分支最近的结果（跳/不跳）
- 执行分支时：查表 → 按预测执行 → 验证 → 若错误，冲刷并更新表项
- 实际中用 **PC 低位几位**作为索引（完整地址当 tag 太贵，几十位 vs 1~2位数据,所以一般取10位, $2^{10}=1024$ 问题不是很大）
- 分支预测通常在 **IF** 阶段开始，如果等到 **ID** 阶段，这时候 IF 里面可能已经是 PC+4 这个下一条指令了，你还得回去刷新
- **IF 阶段怎么判断是否是分支呢？** 直接把当前指令的PC 去 BHT里面查询，要是查到了就直接按照 BHT 里面的记录去预测
### 7.2 1-bit 预测器（1-bit Predictor）⭐

每个表项只有 **1 位**：0 = 预测不跳，1 = 预测跳。
**致命缺陷：嵌套循环会连续出错两次！**

```
outer: ...
inner: ...
       beq ..., ..., inner   ← 内层循环
       ...
       beq ..., ..., outer   ← 外层循环
```

- 内层循环最后一次迭代：预测跳转 → 实际不跳 → **第1次误预测**
- 内层循环下次第一次迭代：预测不跳 → 实际跳转 → **第2次误预测**

### 7.3 ⭐ 2-bit 预测器（2-bit Predictor / FSM）— 必考！

用 **2 位饱和计数器（2-bit Saturating Counter）**，需要**连续两次预测错误**才改变预测方向。

**4个状态（有限状态机 FSM）：**

```
        跳转                 跳转
  ┌─────────────┐      ┌─────────────┐
  ↓             │      ↓             │
 00           ──→ 01 ──→ 10           ──→ 11
强预测不跳        弱预测不跳  弱预测跳转        强预测跳转
  ↑             │      ↑             │
  └─────────────┘      └─────────────┘
       不跳                  不跳
```

|状态码|含义|预测|
|---|---|---|
|`00`|强预测"不跳转"|Not Taken|
|`01`|弱预测"不跳转"|Not Taken|
|`10`|弱预测"跳转"|Taken|
|`11`|强预测"跳转"|Taken|

**优点**：单次误判不改变预测，对嵌套循环只误判一次（而非两次）。

### 7.4 ⭐ 相关预测 / 两级自适应预测（Correlated / 2-level Adaptive Branch Prediction）

也叫"关联预测"，考虑**多个分支之间的相关性**。
应该是比 2-bit 还聪明的东西
#### 核心组件：

**1. 全局分支历史寄存器（Global Branch History Register, GHB）**

- 移位寄存器，记录最近 N 条分支的结果（1=跳，0=不跳）(类似状压DP那样的二进制码)
- 每次有分支执行，结果移入，最旧的移出

**2. 模式历史表（Pattern History Table, PHT）**

- 每个表项是一个 **2-bit 饱和计数器**
- 用 **GHB 的值**作为索引（GHB 是什么模式，就用哪个计数器）
- 跳转时计数器 +1，不跳时计数器 -1
#### 为什么有效？（经典例子 ⭐）

```c
if (a == 2) a = 0;     // Branch 1
if (b == 2) b = 0;     // Branch 2
if (a != b)  { ... }   // Branch 3
```

- 如果 Branch 1 **跳**（a变0）且 Branch 2 **跳**（b变0）
- 则 Branch 3 一定 **不跳**（a == b == 0）
- 然后把 PC 和 GHB 做一个 hash(最经典的方法是**按位异或,Gshare Predicto** ) 就能将其映射成 PHT 的一个索引，然后去查
- GHB = `11` → 查 PHT 的第3项 → 预测不跳 → 正确！
- 单独看 Branch 3，无法预测；但结合前两个分支的历史，可以精准预测

## 八、分支目标缓冲器（Branch Target Buffer, BTB）⭐

### 8.1 "Whether & Where" 问题

分支预测解决了 **"是否跳"（Whether）**，但还需要解决 **"跳到哪里（Where）"**。

即使预测"跳转"，还需要计算目标地址（通常在 ID 或 EX 阶段）——这还会有 1~2 个周期的惩罚。

### 8.2 BTB 工作原理
- **缓存目标地址**：存储历史上跳转的目标 PC 值
- **在 IF 阶段**：用当前 PC 索引 BTB
- 如果命中且预测为"跳转" → 立刻用 BTB 中的地址取下一条指令，**无需等待计算目标地址**

也就是说在 IF 阶段，硬件同时干两件事：
1. 用 PC 查 BHT ，预测是否会跳转
2. 用 PC 查 BTB ，找到跳转的地址，一旦 BHT 预测需要跳转，PC 立刻切换成这个地址

### 8.3 局限
- 间接跳转（Indirect Branch）目标地址会变化，BTB 效果有限。一般用**IBP - Indirect Branch Predictor，间接分支预测器**, 把 PC 和 GHB hash(和前面那个PHT 有点像)
- 函数返回（Return）：通常用专门的**返回地址栈（Return Address Stack, RAS）** 处理
## 九、控制信号在各阶段的传递 ⭐

控制信号在 ID 阶段由主控制器生成，然后随流水线寄存器向后传递：

ID 阶段， Write 寄存器的操作一般来自于上一条指令 WB 回来的

|控制信号|使用阶段|延迟周期|
|---|---|---|
|ALUOp, ALUSrc (A/Bsel, ALUctl)|EX|延迟 1 周期|
|MemRd, MemWr, Branch|MEM|延迟 2 周期|
|MemtoReg, RegWr|WB|延迟 3 周期|

**RegDst（rd目标寄存器编号）也随流水线传递到 WB 阶段。**

流水线寄存器传递路径：

```
IF/ID → ID/EX → EX/MEM → MEM/WB
  ↑        ↑        ↑         ↑
取指      译码     执行       访存
```

## 十、重要公式汇总 ⭐⭐（考试重点！）

### 1. 控制冒险 CPI 惩罚（最常考）

$$\boxed{\text{CPI 惩罚} = N_{\text{flush}} \times P_{\text{branch}} \times P_{\text{taken}}}$$

- $N_{\text{flush}}$：分支结果确定前已取入的错误指令数（MEM确认=3，EX确认=2）
- $P_{\text{branch}}$：分支指令占比（典型值 0.2 即 20%）
- $P_{\text{taken}}$：分支被实际跳转的概率（典型值 0.5 即 50%）

**常用数字：**

| 场景           | 冲刷数 | 惩罚 CPI                  |
| ------------ | --- | ----------------------- |
| 分支在 MEM 阶段判断 | 3   | 3 × 0.2 × 0.5 = **0.3** |
| 分支在 EX 阶段判断  | 2   | 2 × 0.2 × 0.5 = **0.2** |
|              |     |                         |

### 2. 转发检测条件（必须能默写！）

```
EX/MEM 转发（优先级更高）：
  1a. EX/MEM.RegRd == ID/EX.RegRs1  AND  EX/MEM.RegWr  AND  EX/MEM.RegRd ≠ 0
  1b. EX/MEM.RegRd == ID/EX.RegRs2  AND  EX/MEM.RegWr  AND  EX/MEM.RegRd ≠ 0

MEM/WB 转发：
  2a. MEM/WB.RegRd == ID/EX.RegRs1  AND  MEM/WB.RegWr  AND  MEM/WB.RegRd ≠ 0
  2b. MEM/WB.RegRd == ID/EX.RegRs2  AND  MEM/WB.RegWr  AND  MEM/WB.RegRd ≠ 0
```

### 3. 预测"不跳转"策略下的性能（简化版）

$$\text{Total CPI} = 1 + \text{CPI 惩罚（来自各类冒险）}$$

## 十一、知识点速查卡（考前背诵）

### ⭐ 三大冒险一览

|类型|结构冒险|数据冒险|控制冒险|
|---|---|---|---|
|根本原因|硬件资源冲突|数据依赖|分支跳转|
|发生在|任意阶段|主要在 EX/MEM|IF 阶段猜错|
|典型解决|分离 I/D-cache，增加资源|转发+停顿|分支预测+冲刷|

### ⭐ 数据转发来源

- 来自 **EX/MEM 寄存器**：上一条指令刚执行完
- 来自 **MEM/WB 寄存器**：上上条指令执行完

### ⭐ Load-Use 冒险必须停顿1周期

- Load 数据在 MEM 结束才有
- 下一条若立刻用，转发也来不及（时间上不够）
- 必须插入 1 个气泡（stall）

### ⭐ 2-bit 预测器 FSM 记忆口诀

```
00 → 强不跳  01 → 弱不跳  10 → 弱跳  11 → 强跳
|__连续两次错误才切换预测方向__|
```

### ⭐ BHT vs BTB 区别

|类型|BHT（分支历史表）|BTB（分支目标缓冲器）|
|---|---|---|
|解决|是否跳转（Whether）|跳到哪里（Where）|
|内容|1/2 bit 计数器|目标 PC 地址|
|索引|分支指令 PC 低位|当前 PC|

## 十二、关键英文术语对照表

|中文|英文|
|---|---|
|流水线冒险|Pipeline Hazard|
|结构冒险|Structural Hazard|
|数据冒险|Data Hazard|
|控制冒险|Control Hazard|
|流水线停顿|Pipeline Stall|
|流水线气泡|Pipeline Bubble|
|数据转发 / 旁路|Data Forwarding / Bypassing|
|Load-使用冒险|Load-Use Hazard|
|冲刷流水线|Flush Pipeline|
|静态分支预测|Static Branch Prediction|
|动态分支预测|Dynamic Branch Prediction|
|分支历史表|Branch History Table (BHT)|
|1位/2位预测器|1-bit / 2-bit Predictor|
|饱和计数器|Saturating Counter|
|有限状态机|Finite State Machine (FSM)|
|全局分支历史寄存器|Global Branch History Register (GHB)|
|模式历史表|Pattern History Table (PHT)|
|相关/两级自适应预测|Correlated / 2-level Adaptive Branch Prediction|
|分支目标缓冲器|Branch Target Buffer (BTB)|
|哈佛架构|Harvard Architecture|
|冯·诺依曼架构|Von Neumann Architecture|
|编译器静态调度|Compiler Static Scheduling|
|动态调度|Dynamic Scheduling|
|指令重排序|Instruction Reordering|
|指令缓存|Instruction Cache (I-cache)|
|数据缓存|Data Cache (D-cache)|